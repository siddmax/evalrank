# EvalRank Status

Last updated: 2026-07-09

Hosted contract status: the legacy `POST /v1/recommendations` operation is temporarily unavailable and returns RFC 9457 code `recommendation_not_published`. It must not return a cached ranking that ignores the request; the deterministic decision API will replace it atomically.

## Full-Spec Dashboard

This section reports progress against the full private EvalRank spec corpus and W0-W9 implementation plan without copying private source text into this public repo. Percentages are implementation-coverage estimates, not confidence or business-readiness scores.

Current gate: W6 proof-of-spine is verified privately and the current default-agent lane now has promotion-quality Claude Opus plus active measured Codex evidence. The separate Syndai worktree ingested a current-schema full golden snapshot for `claude_code / claude-opus-4-8`: `dry_run=false`, `partial_run=false`, `task_count=20`, `pass_count=20`, `pass_rate=1.0`, category pass rates `coding_repair=10/10`, `site_clone=5/5`, `review_only=5/5`, governance rows present where expected, benchmark/retrieved text present for all 20 cases, and `cost_per_task_usd_p50=0.049259`. It also ingested a full Codex snapshot for `codex_cli / gpt-5.5`: `dry_run=false`, `task_count=20`, `pass_count=19`, `pass_rate=0.95`, category pass rates `coding_repair=10/10`, `site_clone=4/5`, `review_only=5/5`, and unknown cost preserved as `null`. Private promotion produced active router snapshot `9d629dd4-8b0e-40c9-ada1-c2b6d33790e0`, projected EvalRank snapshot `coding-router-active-9d629dd4`, rebuilt item responses, contamination, Stage-2, Stage-4, and materialized cache `rec_bc07b8cef6253fa87584049a`. The private worktree also ingested a separate candidate-ledger calibration source from the existing `claude_code / claude-sonnet-4-6` full-bank baseline (`dry_run=false`, `task_count=20`, `pass_count=12`, `pass_rate=0.6`) without promoting it into active routing. Canonical cross-source case item ids let the active snapshot plus candidate-ledger snapshot form a comparable 3-entity, 9-informative-item, 27-binary-response agent-lane matrix. The pooled IRT fit `irt_6d3eb1deb9ff796f74461222` is publishable with `max_r_hat=1.003291`, `min_effective_sample_size=1167.966431`, `divergence_count=0`, `sbc_publishable=true`, and `sbc_rank_count=210`. Stage-2 now consumes pooled calibration only through explicit `calibration_source_name`/`calibration_snapshot_id` inputs, while active route evidence remains scoped to `syndai-coding-router-evidence / coding-router-active-9d629dd4`. The private eval-pipeline worker now has a replayable non-dry cache-refresh helper over that scorer/materializer spine: target proof emitted active snapshot `coding-router-active-9d629dd4`, candidate calibration snapshot `coding-router-candidate-ledger-6791d009d9f4`, pooled snapshot `autonomous-swe-agent-pooled-active-9d629dd4`, publishable fit `irt_c732863fe624e0a670340ad6`, and active cache `rec_cc32bb34a0b4afa8de58435a`. The rebuilt active cache ranks Claude Opus first (`stage2_calibrated=0.709694`) and Codex GPT-5.5 second (`0.679625`); Claude Sonnet is calibrated but outside the Stage-4 shortlist (`0.094553`), and Pi is outside the active cache with `required_calibration_missing_v1`, `score=0.0`, and `confidence=0.0`. The private worktree now also has cache-backed W7 read routes: `POST /v1/recommendations`, `POST /api/v1/recommendations`, and `GET /api/v1/recommend?use_case=...` return the active materialized payload unchanged and emit `recommend.called` from that same payload. Those routes remain anonymous-first, bearer-key callers now resolve through Syndai's existing customer API-key control plane with `evalrank:recommendations:read`, keyed external telemetry, successful key-usage persistence, and the same emitted `recommend.called` payload is persisted into append-only private table `evalrank.recommend_events`; keyed callers can now read current-period usage from those facts at private route `GET /api/v1/evalrank/usage/current-period`, with visible quota state from `evalrank.recommendation_quota_limits` when configured, and configured exhausted keyed quotas now return `429` with retry/quota headers plus an explicit cached recommendation fallback instead of live reranking or silently dropping the answer. It now also has a private tool-registry capability source adapter that projects 100 live metadata-coverage candidates into the private `evalrank` schema, preserves upstream source fingerprints in entity/evidence/result provenance, and materializes cache `rec_b45288cb38b8bbce2cfbc836` with source-snapshot lineage plus stale-write protection. The private worktree also projects raw registry-entry coverage from `syndai.tool_registry` as `tracking-only` metadata, with balanced API/CLI/MCP/skill source-family rows and served cache caveats that explicitly say metadata tracking is not performance evidence. Private source adapters in Syndai may read `syndai.*` source tables during incubation, but derived EvalRank rows, migrations, grants, RLS, and caches stay in the separate private `evalrank` schema; shared customer API-key scope changes stay in `syndai` because they are auth control-plane infrastructure, not EvalRank persistence. The private worktree now also has the first hard budget reservation gate for async expensive scoring work: `evalrank.budget_ledger` plus a conditional `UPDATE ... RETURNING` helper that returns `budget.reserved` or `budget.ceiling_reached` before future eval/synthesis/model-swap/GTM dispatchers enqueue spend. It now also has an append-only private source lifecycle event ledger: `evalrank.source_evidence_events` plus coding-router projections/sweep wiring that record explicit `snapshot_active`, `observed`, `promoted`, `stale_removed`, and replacement-snapshot `superseded` source facts instead of inferring row lifecycle from downstream caches. Pi remains wired but is not used as the default harness or rewarded in the active cache until it has comparable calibration evidence and passes the behavior gate. The next full-spec gate is broader private source-adapter coverage with budget-gated behavior-eval promotion, then W7 hosted/staging plus billing settlement proof over the verified worker-backed scorer/materializer spine.

Latest full-suite validation before W7 route-parity slice: on 2026-06-27, public `/Users/sidsharma/evalrank` `make check` passed with 223 Python tests plus TypeScript SDK syntax/test checks, and public `git diff --check` passed. Private `/Users/sidsharma/Syndai/.wt/codex-evalrank-w6-spine` `make check` passed after formatting, lint, type checking, static security guards, file-size checks, migration-boundary checks, and repository audits; `make eval-coding-deterministic` passed with `240 passed, 3 skipped`; private `git diff --check` passed. Private focused cached recommend API/telemetry tests passed with `27 passed`, covering active cache reads, `GET /api/v1/recommend?use_case=...`, `recommend.called` event construction, active-span attributes, 404 missing-cache behavior, and app DI registration. Private target-DB checks ingested Codex full-bank rows through the existing golden-eval pipeline writer, promoted 3 measured Codex rows while carrying forward 9 existing active rows, projected 4 stage candidates, 67 result rows, 40 task results, 61 active item responses, 268 contamination rows, 4 Stage-2 rows, and 4 Stage-4 rows, skipped IRT for the correct sparse-matrix reason, and materialized `rec_bc07b8cef6253fa87584049a` with Claude Opus first and Codex GPT-5.5 second. The candidate-ledger slice then applied migration `2026_06_27_020_evalrank_item_response_replace_grant`, ingested 3 Sonnet candidate rows, projected candidate source snapshot `coding-router-candidate-ledger-2026-06-27T114700Z`, projected 26 candidate item responses, rebuilt 61 active item responses with source-independent case ids, and fit publishable pooled IRT/SBC over active plus candidate snapshots. The pooled-calibration consumption slice then rebuilt 4 Stage-2 rows with explicit pooled calibration, rebuilt 4 Stage-4 rows, and materialized `rec_93e4123d3ed37f1075e11544` with Claude Opus first, Codex GPT-5.5 second, and Pi excluded by required-missing calibration. The ingestion-worker cache-refresh slice then proved the same active ranking through the private worker helper, emitting fit `irt_c732863fe624e0a670340ad6` and cache `rec_cc32bb34a0b4afa8de58435a` from deterministic active/candidate/pooled snapshot ids. The tool-registry capability source-adapter/cache-lineage slice then passed 82 focused pytest regressions, focused ruff and ty checks, the EvalRank migration-boundary check, applied migration `2026_06_27_022_evalrank_recommendation_cache_snapshot_lineage`, projected 100 live tool-registry metadata candidates into `evalrank`, materialized cache `rec_b45288cb38b8bbce2cfbc836`, and verified 100 candidates, 100 evidence rows, 100 result rows, 64-hex EvalRank candidate ids, upstream source fingerprints preserved in entity/evidence/result provenance, active source-snapshot cache lineage, and `base_snapshot_lag_ms=0`.

W7 route-parity update: on 2026-06-27, the private worktree added cache-backed `POST /v1/recommendations` for the public SDK/CLI/MCP base-url shape plus `POST /api/v1/recommendations` for the Syndai API-prefix alias, both sharing the existing active-cache and `recommend.called` path with `GET /api/v1/recommend?use_case=...`. Red/green route regressions proved both POST paths returned 404 before implementation and passed after implementation; the focused cached recommend API/telemetry lane passed with `30 passed`, and focused ruff plus ty checks passed for the touched controller/test files. Follow-up local live proof started the private API with Granian on `127.0.0.1:8017`: `curl` returned `200 OK` for `POST /v1/recommendations` and `GET /api/v1/recommend?use_case=autonomous-swe-agent`, returned `400 Bad Request` for a malformed POST, and public Python SDK, CLI, MCP, and TypeScript SDK clients all returned `rec_cc32bb34a0b4afa8de58435a` with Claude Opus first. A first Python SDK check with a 15s timeout timed out while the server later logged a 200 after about 20.8s on the local-to-Doppler/Supabase path, so this is local route/client parity evidence, not production latency proof. Quota/billing, hosted/staging proof, and production-latency/load proof remain W7 work.

W7 optional API-key auth update: on 2026-06-28, the private worktree kept cached recommendation reads anonymous-first and added optional bearer-key resolution through Syndai's existing customer API-key control plane with scope `evalrank:recommendations:read`. Successful keyed reads emit `recommend.called` as external/keyed and persist key usage; anonymous reads stay anonymous. The scope migration updates Syndai's `syndai.caas_plans` and `syndai.customer_api_keys` constraints and plan seeds because this is shared identity infrastructure, not EvalRank persistence. The focused private route/auth/migration/docs lane passed with `52 passed`, focused ruff and ty passed, `scripts/validate_migrations.py` passed for the new Syndai migration, and `scripts/check_evalrank_migration_boundary.py` passed. Full gates also passed: public `/Users/sidsharma/evalrank` `make check` with 223 Python tests and 7 TypeScript SDK tests, private `/Users/sidsharma/Syndai/.wt/codex-evalrank-w6-spine` `make check` after formatting/lint/type/security/repo-documentation checks, and `git diff --check` in both repos.

W7 usage-summary update: on 2026-06-28, the private worktree added keyed-only current-period usage aggregation over append-only `evalrank.recommend_events`. Private route `GET /api/v1/evalrank/usage/current-period` requires the existing `evalrank:recommendations:read` customer API-key scope, returns current UTC calendar-month bounds, total/keyed/unique/domain counts, top-domain counts, and visible quota state. The consumer-period index was added in new EvalRank migration `2026_06_28_029_evalrank_recommend_events_usage_index` rather than mutating the already-applied fact-table migration. The private quota source was added in migration `2026_06_28_030_evalrank_recommendation_quota_limits`; rows live in the dedicated private `evalrank` schema with RLS/grants and drive `quota.enforced`, `quota.limit`, and `quota.remaining` when configured. Focused private helper/controller/migration tests passed with `9 passed` for the usage-index slice and `5 passed` for the visible-quota source slice, focused ruff and ty passed, the private EvalRank migration-boundary check passed, target migration replay reported EvalRank migrations already current for the usage-index slice, and target `pg_indexes` readback verified `evalrank_recommend_events_consumer_period` on `(consumer_class, consumer_id, occurred_at DESC)`. Full local gates also passed for both slices: private `/Users/sidsharma/Syndai/.wt/codex-evalrank-w6-spine` `make check`, public `/Users/sidsharma/evalrank` `make check` with 223 Python tests plus 7 TypeScript SDK tests, focused private usage tests after formatting, `git diff --check` in both repos, and explicit touched-file trailing-whitespace scans. Public-safe summaries: `docs/build-log/2026-06-28-private-w7-usage-summary.md` and `docs/build-log/2026-06-28-private-visible-quota-source.md`.

W7 keyed quota enforcement update: on 2026-06-28, the private worktree added request-path enforcement for keyed cached recommendation reads. It reuses private `evalrank.recommend_events` and `evalrank.recommendation_quota_limits`; callers with `quota_enforced=true` and no remaining monthly reads receive `429` with `Retry-After`, rate-limit/quota headers, and an explicit cached recommendation fallback payload. Anonymous and first-party reads keep the existing active-cache path, and exhausted keyed reads do not write a successful `recommend.called` fact. Focused red regressions failed first for the missing quota-state helper and route branch; focused private controller/usage tests then passed with `10 passed`, the adjacent EvalRank unit/source lane passed with `160 passed`, focused ruff and ty passed, and the private EvalRank migration-boundary check passed. Public-safe summary: `docs/build-log/2026-06-28-private-keyed-quota-enforcement.md`.

W7 billing settlement update: on 2026-06-28, the private worktree added async billing settlement/readback over successful keyed external `evalrank.recommend_events` and wired due settlement dispatch into the existing coding webhook worker. Ranking requests still never call Stripe; settlement rows live in private `evalrank.recommend_billing_settlements`, and the worker records provider event/invoice/receipt readback after asynchronous meter-event dispatch. Focused private worker/billing tests passed with `9 passed`, focused ruff and ty checks passed, the private migration-boundary check passed, private `make check` passed, and public `make check` passed with 223 Python tests plus 7 TypeScript SDK tests. Follow-up target DB proof applied EvalRank migrations through `2026_06_28_031_evalrank_recommend_billing_settlements`, verified 2 RLS policies and 3 indexes, found 2 recommendation facts but 0 billable keyed external facts, and ran a rollback-only synthetic enqueue proof that left 0 synthetic recommend events and 0 synthetic settlement rows. Public-safe summary: `docs/build-log/2026-06-28-private-w7-billing-settlement.md`.

W7 private billing readback route update: on 2026-06-29, the private worktree added authenticated `GET /api/v1/evalrank/billing/readbacks` over the existing private settlement readback helper. The route requires the existing `evalrank:recommendations:read` API-key scope, bounds `limit` at 1-100, returns only the authenticated customer's private settlement rows, and does not add a public receipt contract or Stripe call in the ranking/request path. Red route proof failed first with `404`; focused private controller/readback tests then passed with `15 passed`. Public-safe summary: `docs/build-log/2026-06-29-private-w7-billing-readback-route.md`.

W7 Stripe setup profile update: on 2026-06-29, the private worktree extended the existing Stripe meter setup helper with an explicit EvalRank recommendation profile. It can create or reuse the `evalrank_recommendation` Billing Meter, `EvalRank Recommendations - Metered` product, and attached monthly metered price once a real Stripe key and chosen cents-per-recommendation price exist. The existing no-arg Syndai outcome setup remains the default, and the EvalRank profile requires explicit `--unit-amount-cents` so live pricing is not hidden in code. Focused private setup tests passed with `3 passed`, and focused ruff/ty passed. No Stripe network call was made. Public-safe summary: `docs/build-log/2026-06-29-private-w7-stripe-evalrank-meter-setup-profile.md`.

W7 staging smoke command update: on 2026-06-28, the private worktree added an account-free local/staging smoke command for the existing recommendation read surface. It checks `POST /v1/recommendations`, `POST /api/v1/recommendations`, and `GET /api/v1/recommend?use_case=...` for the same cached recommendation id and top-ranked entity, and optionally checks keyed current-period usage when an API key is supplied through an environment variable. Focused private smoke tests passed with `3 passed`, focused ruff and ty passed, and the help command rendered successfully. This is the reusable smoke harness; the hosted/staging gate remains open until it is run against deployed staging with real HTTP responses. Public-safe summary: `docs/build-log/2026-06-28-private-w7-staging-smoke-command.md`.

W8 OTel semantic-attribute update: on 2026-06-28, the private worktree standardized successful cached recommendation-read spans on official OpenTelemetry HTTP server attributes: `http.request.method`, `http.route`, and `http.response.status_code`. The existing `evalrank.*` span fields remain, and a safe GenAI helper now emits only non-content operation/model/token attributes for future scorer/judge spans. Cached recommendation reads intentionally do not set `gen_ai.*` because they are served from the materialized cache rather than live model inference. Focused private OTel/telemetry/controller tests passed with `15 passed`, focused ruff and ty passed. Public-safe summary: `docs/build-log/2026-06-28-private-w8-otel-semantic-attributes.md`.

W8 recommend-event daily rollup update: on 2026-06-28, the private worktree added a derived daily SLI rollup table over append-only `evalrank.recommend_events`. The helper refreshes half-open UTC date windows into rows grouped by date, domain, surface, consumer class, and status, with recommendation counts, keyed counts, unique consumers, ranked-row sums, and p50/p95 latency summaries. It deletes only derived rollup rows for the refresh window and rebuilds from immutable raw facts. Focused private helper/migration tests passed with `7 passed`, focused ruff and ty passed, the private migration-boundary check passed, and target DB proof applied the migration, verified 13 columns, 2 RLS policies, 3 indexes, and refreshed 1 daily rollup row from 2 existing recommendation facts. Public-safe summary: `docs/build-log/2026-06-28-private-w8-recommend-event-daily-rollups.md`.

W8 recommendation load SLO smoke update: on 2026-06-28, the private worktree added an account-free local/staging load smoke command for cached recommendation reads. The command sends a bounded concurrent batch to `POST /v1/recommendations`, verifies stable cached recommendation and top-ranked ids, reports p50/p95/max latency, and exits nonzero when configured p50/p95 SLOs are missed. Focused private command tests passed with `3 passed`, focused ruff and ty passed, and the help command rendered successfully. This is the reusable latency gate; hosted/staging or production-like latency proof remains open until it is run against deployed infrastructure. Public-safe summary: `docs/build-log/2026-06-28-private-w8-recommendation-load-slo-smoke.md`.

W7/W8 staging smoke nonlocal guard update: on 2026-06-29, the private worktree added an opt-in `--require-non-local-base-url` guard to the W7 staging parity smoke and W8 recommendation load smoke commands. Local development defaults remain unchanged, but any run intended as hosted/staging proof now rejects localhost, loopback, private, link-local, reserved, multicast, unspecified IP hosts, `.localhost`, and `.local` hostnames before network I/O. Focused private red/green parser tests passed with `14 passed`, and focused ruff passed for the touched scripts/tests. This is proof-quality hardening only; the actual hosted/staging gate still requires a deployed base URL plus API key. Public-safe summary: `docs/build-log/2026-06-29-private-w7-w8-staging-smoke-nonlocal-guard.md`.

W7 disclosure access-tier gate update: on 2026-06-29, the private worktree added a per-use-case disclosure tier so the public/SEO surface shows only curated generic "marquee" categories while the long tail of specific use cases requires a key. A new EvalRank migration adds a curated `evalrank.use_case_access` table (`access_tier` default `api`, RLS/grants) seeded with the known generic use cases as `public`; the active-cache reader LEFT JOINs it (one query, coalesce to `api`), and the recommend route returns `404` for anonymous callers on `api`-tier use cases so the gated catalog cannot be enumerated, while keyed callers get the full catalog. `access_tier` is orthogonal to the existing `publishable` evidence-depth filter. Focused gate/reader tests passed; the full private EvalRank unit lane passed. Public-safe summary: `docs/build-log/2026-06-29-private-disclosure-moat-and-overage-billing.md`.

EvalRank pricing/disclosure decision pin: on 2026-06-29, the private decisions pinned the billing shape (no seats; flat tier + included recommendation quota + hard-cap-by-default + opt-in self-capped overage at 1¢/recommendation; never affects rank) and the disclosure surface tiers (marquee public vs API-only, split by category granularity). A cached-read financial model (~96% contribution margin; profit gated by the periodic eval-pipeline recompute, not the per-rec rate) backs the rate. These are private business decisions; only product-neutral notes belong in this public repo. Public-safe summary: `docs/build-log/2026-06-29-private-disclosure-moat-and-overage-billing.md`.

W7 overage billing update (Model A): on 2026-06-29, the private worktree made within-included-quota keyed reads free and metered only opted-in overage. EvalRank billing now resolves a test or live Stripe key (`STRIPE_TEST_KEY` else `STRIPE_SECRET_KEY`), so it bills test-mode during incubation and live in production; the shared Stripe REST helpers gained a backward-compatible `api_key` parameter so Syndai outcome billing is unchanged. A new EvalRank migration adds `overage_enabled`/`overage_monthly_cap_usd` to `evalrank.recommendation_quota_limits` and a `billable` flag (plus partial index) on `evalrank.recommend_events`. Past the included quota the route hard-stops by default; opted-in callers under their monthly USD cap are served and marked billable, settlement enqueues only billable facts, and a stateless idempotent subscription guard ensures the metered price is attached before meter events post. The full private EvalRank plus coding-Stripe lanes passed with `220 passed`, with focused ruff, ty, and the EvalRank migration-boundary check green; end-to-end test-mode Stripe and hosted smoke proof remain open until deploy. Public-safe summary: `docs/build-log/2026-06-29-private-disclosure-moat-and-overage-billing.md`.

Preflight EvalRank-migration coverage update: on 2026-06-29, the private worktree extended the preflight migration safety net to also cover `backend/evalrank_migrations/` (previously only the Alembic `backend/migrations/` path). The schema-touch detector and the live-DB migration check now flag EvalRank raw-SQL migration changes and remind operators that EvalRank migrations are applied via `apply_evalrank_migrations.py` and are not auto-applied on deploy. Public-safe summary: `docs/build-log/2026-06-29-private-disclosure-moat-and-overage-billing.md`.

W8 daily observability rollup scheduling update: on 2026-06-28, the private worktree scheduled private daily recommendation SLI rollups through the existing durable `pg_cron -> pgmq -> eval_pipeline_worker` path. A new `evalrank_observability_rollup` message type reuses the private `evalrank_source_refresh_jobs` queue; the worker refreshes the recent UTC rollup window, archives successful jobs, leaves failures for visibility-timeout retry, and moves unsupported/poison messages to DLQ. Focused private cron/worker/source-of-truth tests passed with `39 passed`, focused ruff and ty passed, migration validation passed, target migration applied, and target readback verified `evalrank-observability-rollup` on `15 5 * * *` using `pgmq.send` with no HTTP/pg_net. Public-safe summary: `docs/build-log/2026-06-28-private-w8-daily-observability-rollup-scheduling.md`.

W8 live verdict UI update: on 2026-06-28, the private worktree added the first authenticated Syndai web surface for the cached EvalRank recommendation path. A new private BFF route forwards the user's existing Supabase bearer session server-side to backend `POST /api/v1/recommendations` with private/no-store responses; the browser does not receive backend base URLs, provider tokens, API keys, or DB details. The authenticated `/[locale]/evalrank` page renders recommendation id, methodology/cache metadata, top candidates, BFF failures, and keyed-quota cached fallback bodies. Focused private web route/component tests passed with `4 passed` plus `4 passed`. This is a private web proof only, not a public EvalRank UI contract. Public-safe summary: `docs/build-log/2026-06-28-private-w8-live-verdict-ui.md`.

W8 live verdict home-link update: on 2026-06-28, the private worktree made the authenticated live verdict page discoverable from the Syndai authenticated home page. The home CTA links to `/[locale]/evalrank` after sign-in while preserving the existing connection-management path. Focused private home/BFF/panel tests passed with `9 passed`. This remains private UI only; no public EvalRank navigation contract changed. Public-safe summary: `docs/build-log/2026-06-28-private-w8-live-verdict-home-link.md`.

W9 WAQC weekly rollup update: on 2026-06-28, the private worktree added the first W9 north-star metric substrate over private append-only recommendation facts and scheduled it through the existing durable worker queue. The private rollup counts headline WAQC from distinct external keyed consumers with successful recommendation calls, keeps first-party dogfood and anonymous diagnostics separate, and refreshes rebuildable UTC Monday-week windows from raw facts so the metric can be backfilled without approximating from daily aggregates. Focused private helper/migration tests passed with `4 passed`; focused private cron/worker/source-of-truth tests passed with `45 passed`; target readback verified the rollup shape and weekly `evalrank-waqc-rollup` cron job on `30 5 * * 1` with `pgmq.send` and no HTTP command. Public-safe summary: `docs/build-log/2026-06-28-private-w9-waqc-weekly-rollups.md`.

W9 WAQC regression-event update: on 2026-06-28, the private worktree added a human-gated regression-event substrate over the weekly WAQC rollups. The detector compares the latest completed external WAQC week with the prior completed week, writes deterministic private `waqc_drop` facts with `draft_pr_required=true` only on real drops, and leaves GitHub draft-PR dispatch, Slack, and GTM provider side effects unimplemented/account-gated. Focused private regression/migration/worker tests passed with `4 passed` plus `1 passed`; the combined W8/W9 focused lane passed with `53 passed`; private ruff, ty, migration-boundary, and migration-validation checks passed; target private DB migration/readback verified 20 columns, 2 RLS policies, 4 indexes, three refreshed WAQC weeks, and no fabricated regression event for the real zero-count comparison. Public-safe summary: `docs/build-log/2026-06-28-private-w9-waqc-regression-events.md`.

W9 regression draft-PR artifact update: on 2026-06-28, the private worktree added a provider-free local artifact builder for WAQC regression follow-up. The helper and command turn sanitized `waqc_drop` regression metadata into a draft-only GitHub create-PR payload, run it through the existing publication-governance gate, and print JSON for a later provider dispatcher. No GitHub API call, branch push, remote PR, Slack alert, or GTM posting occurred. Focused private helper/command tests passed with `6 passed`, focused ruff and ty passed, and a real command invocation emitted a `draft=true` payload for `siddmax/Syndai`. Public-safe summary: `docs/build-log/2026-06-28-private-w9-regression-draft-pr-artifact.md`.

W9 affiliation-gated draft-PR update: on 2026-06-28, the private worktree tightened that draft-PR artifact path so it now requires the private operator-affiliation completeness check before emitting JSON. The command requires `DATABASE_URL` or `--database-url`, blocks missing affiliation rows, includes affiliation/COI gate text in the generated draft body, and marks operator-COI disclosure as required in the private publication-governance decision. Focused private helper/command tests passed with `9 passed`, and focused ruff and ty passed. Public-safe summary: `docs/build-log/2026-06-28-private-w9-affiliation-gated-draft-pr.md`.

W9 DB-backed regression draft-PR update: on 2026-06-28, the private worktree extended the same local artifact command so operators can build the gated draft-PR JSON from an open private `evalrank.regression_events` row by regression id instead of hand-copying regression values into JSON. DB-backed payloads still require a real evidence URL or configured URL base, and the existing operator-affiliation plus publication-governance gates still run before JSON emission. Focused private helper/command tests passed with `12 passed`, and focused ruff and ty passed. Public-safe summary: `docs/build-log/2026-06-28-private-w9-db-backed-regression-draft-pr.md`.

W9 regression draft-PR dispatch-readback update: on 2026-06-28, the private worktree added a local `--record-dispatched-url` mode to the draft-PR artifact command so future provider dispatchers/operators can record the created draft PR URL back onto the private regression event row without hand SQL. The mode validates the URL, sets private status/readback metadata, and emits sanitized JSON only. Focused private helper/command tests passed with `15 passed`, and focused ruff and ty passed. Public-safe summary: `docs/build-log/2026-06-28-private-w9-regression-draft-pr-dispatch-readback.md`.

W9 publication-governance tripwire update: on 2026-06-28, the private worktree added a provider-free pre-publication governance/legal gate for future GTM, report, social, and draft-PR dispatchers. The checker blocks missing operator-COI disclosure, missing material-connection disclosure, unsupported negative labels, negative labels without dated evidence, security labels without CVD clearance, and contested social drafts without human review; the audit writer stores sanitized gate metadata only and no draft body text. Focused private governance/migration tests passed with `8 passed`; private ruff, ty, migration-boundary, and migration-validation checks passed; target private DB migration/readback verified 9 columns, 2 RLS policies, 3 indexes, and a rollback-only blocked social-post audit proof that left 0 synthetic rows. Public-safe summary: `docs/build-log/2026-06-28-private-w9-publication-governance-tripwires.md`.

W9 publication-governance check-command update: on 2026-06-28, the private worktree exposed the pre-publication governance tripwire as a runnable private operator/CI command. The command reads sanitized governance metadata JSON, rejects draft body/content fields, exits nonzero on blocking reasons, and optionally records only sanitized decision metadata when an explicit private DB write is requested. Focused private command tests passed with `6 passed`, focused ruff and ty passed, and a real no-record CLI invocation returned `PASS: publication governance allowed draft draft-cli-pass`. Public-safe summary: `docs/build-log/2026-06-28-private-w9-publication-governance-check-command.md`.

W9 operator-affiliations registry update: on 2026-06-28, the private worktree added the private `evalrank.operator_affiliations` registry and a DB-backed completeness check over current Syndai-operated agent candidates. The registry records operated/funded/commercial-data relationship rows inside the dedicated private `evalrank` schema; the helper reports any `evalrank.stage_candidates` agent entity matching `agent:syndai-%` with no registry row so future CI/dispatchers can fail closed before public COI disclosures. Focused private helper/migration tests passed with `3 passed`; private ruff, ty, migration-boundary, and migration-validation checks passed; target private DB migration/readback verified 5 columns, 2 RLS policies, 2 indexes, refreshed 5 real Syndai-operated candidate registry rows from current candidate data, and the helper returned `is_complete=True`. Public-safe summary: `docs/build-log/2026-06-28-private-w9-operator-affiliations-registry.md`.

W9 operator-affiliations check-command update: on 2026-06-28, the private worktree added a runnable DB-backed operator/CI gate for the affiliation registry. `backend/scripts/check_evalrank_operator_affiliations.py` exits nonzero with missing entity ids if any current Syndai-operated `evalrank.stage_candidates` agent row lacks a private registry entry. Focused private script tests passed with `3 passed`, focused ruff and ty passed, and target private check returned `PASS: all Syndai-operated EvalRank candidate entities have operator affiliations`. Public-safe summary: `docs/build-log/2026-06-28-private-w9-operator-affiliations-check-command.md`.

W9 community listener fetch update: on 2026-06-28, the private worktree extended the existing GTM community-review ingestion command with account-free public metadata fetch mode for Hacker News and Reddit queries. The command normalizes only thread/post id, URL, title, author, and source query into the private review queue; it does not store body/comment/reply text, create accounts, schedule jobs, send email or Slack, draft replies, post publicly, or add any public EvalRank surface. Focused private script tests passed with `6 passed`. Public-safe summary: `docs/build-log/2026-06-28-private-w9-community-listener-fetch.md`.

W9 community digest export update: on 2026-06-28, the private worktree added a provider-free digest exporter for pending community-review rows. The command renders metadata-only Markdown or JSON for human review without mutating queue status, sending email or Slack, posting publicly, creating accounts, scheduling jobs, drafting replies with an LLM, or adding any public EvalRank surface. Focused private exporter tests passed with `5 passed`. Public-safe summary: `docs/build-log/2026-06-28-private-w9-community-digest-export.md`.

W9 community digest delivery update: on 2026-06-29, the private worktree added an explicit provider-delivery wrapper for the existing metadata-only community digest. The command defaults to no provider side effects, requires explicit Slack/Resend send flags, reads secrets only from environment variables, preserves Resend idempotency, and still avoids social posting, LLM reply drafting, queue mutation, account setup, browser use, live provider calls, or public EvalRank surfaces. Focused private delivery tests passed with `7 passed`; the adjacent community digest/export/fetch lane passed with `18 passed`; focused private ruff and ty passed. Public-safe summary: `docs/build-log/2026-06-29-private-w9-community-digest-delivery.md`.

Coding dogfood verified-run adapter update: on 2026-06-28, the private worktree added an offline source adapter for aggregate terminal coding dogfood outcomes. The adapter maps `sentry_auto` terminal dogfood outcomes plus execution-attempt cost aggregates into obfuscated private EvalRank agent rows with score basis `verified_dogfood_terminal_outcomes`; raw executor/model names are not copied into served entity ids. Focused adapter/script/migration tests passed with `5 passed`, the expanded source/materializer/migration lane passed with `95 passed`, focused ruff and ty passed, `scripts/check_evalrank_migration_boundary.py` passed, private and public `make check` passed, `git diff --check` passed in both repos, and target DB migration `2026_06_28_026_evalrank_coding_dogfood_outcome_benchmark` applied. Target projection then failed closed with `no coding dogfood outcome rows found`, so this is adapter-ready and benchmark-catalog-ready but not live source coverage or cache proof until terminal `sentry_auto` dogfood rows exist.

Coding validation outcome adapter update: on 2026-06-28, the private worktree added a non-empty live behavior source adapter over aggregate coding validation outcomes. The adapter maps private validation outcomes into obfuscated EvalRank agent rows with score basis `coding_validation_pass_rate`; raw executor/model names, run ids, validator output, and customer identifiers are not copied into EvalRank rows or this public repo. Focused adapter/script/migration tests passed with `5 passed`, the expanded source/materializer/migration lane passed with `99 passed`, focused ruff and ty passed, `scripts/check_evalrank_migration_boundary.py` passed, target DB migration `2026_06_28_027_evalrank_coding_validation_outcome_benchmark` applied, target projection wrote 3 stage candidates / 3 evidence rows / 3 result rows for `coding-validation-outcomes-2026-06-28`, target materialization emitted `rec_aaab288a1d36c339084ab725` with 3 candidates, standardized trust, and no caveats, private and public `make check` passed, and `git diff --check` passed in both repos.

Coding validation item-response update: on 2026-06-28, the private worktree added a calibration bridge from terminal coding-validation rows into active coding-router agent identities. Aggregate validation rows remain obfuscated for source visibility, while terminal validation item rows use the same active `agent:syndai-coding:*` entity ids as the served router candidates and write item responses before pooled IRT. The bridge reuses `evalrank.result_rows` and `evalrank.item_response_rows`, adds no schema, keeps served cache materialization restricted to `coding-router-active-*`, and does not copy raw commands, outputs, or customer data. Red regressions failed first for missing item-row input, missing item-response writing, and missing cache-refresh ordering; focused private validation/cache-refresh/projection tests then passed with `8 passed`, the broader adjacent source/projection lane passed with `30 passed`, focused ruff and ty passed, and `scripts/check_evalrank_migration_boundary.py` passed. Full local gates also passed: private `/Users/sidsharma/Syndai/.wt/codex-evalrank-w6-spine` `make check`, public `/Users/sidsharma/evalrank` `make check` with 223 Python tests plus 7 TypeScript SDK tests, `git diff --check` in both repos, and explicit touched-file trailing-whitespace scans.

Recommend-called fact ingestion update: on 2026-06-28, the private worktree added append-only `evalrank.recommend_events` and writes the same `recommend.called` payload from successful cached recommendation reads into that private fact table before committing the request. This is intentionally same-session and synchronous for pre-production accuracy: losing the fact is worse than a local request failure, and future traffic pressure can be handled after behavior is proven. Focused writer/controller/migration tests passed with `9 passed`, the cached recommend API/telemetry lane passed with `35 passed`, the EvalRank read-scope migration lane passed with `2 passed`, focused ruff and ty passed, `scripts/check_evalrank_migration_boundary.py` passed, target DB migration `2026_06_28_028_evalrank_recommend_events` applied, target writer proof inserted `evt_evalrank_fact_proof_f1ce417f93b74e1b8d19` with domain `autonomous-swe-agent`, surface `api`, and one ranked entity, private and public `make check` passed, and `git diff --check` passed in both repos.

Served-cache source-boundary update: on 2026-06-28, the private worktree added a materializer guard so `autonomous-swe-agent` served caches must come from `coding-router-active-*` snapshots by default. Auxiliary validation, dogfood, or proof snapshots now require an explicit private override before they can replace the active served cache. A target DB check found the active `autonomous-swe-agent` cache on auxiliary validation snapshot `coding-validation-outcomes-2026-06-28`; the fix rejected that snapshot by default, then replayed the coding-router refresh chain at `2026-06-28T04:22:20Z` and restored active cache `rec_230030790dc52449ed7625be` on `coding-router-active-9d629dd4`.

Worker-owned source-refresh scheduling update: on 2026-06-28, the private worktree moved EvalRank source refresh from an eval-worker sleep loop to Syndai's existing `pg_cron -> pgmq -> worker` runtime pattern. The worker reconciles cron on boot, consumes durable `evalrank_source_refresh_jobs` messages, archives only after a successful pipeline/cache refresh, moves exhausted poison messages to the shared DLQ, and projects coding-router lifecycle facts inside the cache refresh before active materialization. Target DB proof applied `2026_06_28_002_eval_pipeline_pgcron_bridge`, verified queues `evalrank_source_refresh_jobs` and `evalrank_source_refresh_jobs_dlq`, and verified active cron job `evalrank-source-refresh` on `0 5 * * 1`.

Auxiliary source-refresh projection update: on 2026-06-28, the private worktree added projection-only auxiliary source refreshes for tool-registry capabilities, raw registry entries, coding-validation outcomes, and coding dogfood verified runs after successful non-dry coding-router cache refreshes. These refreshes create private source snapshots only; they cannot replace the served `autonomous-swe-agent` cache because the materializer still requires `coding-router-active-*` snapshots by default. Target DB proof at `2026-06-28T13:00:00Z` projected 303 real auxiliary candidates and skipped 1 empty dogfood source with `no coding dogfood outcome rows found`.

Dogfood bootstrap audit update: on 2026-06-28, the private Syndai worktree added a read-only audit subcommand to the existing Sentry provisioner so operators can see why terminal `sentry_auto` dogfood evidence is not yet projectable. Target audit for `eternex / SYNDAI_BACKEND` and `siddmax/Syndai` found repository `f568bbbb-fcb4-46c3-8fc2-04e530eeb805`, no Sentry binding, no triggers, 0 terminal outcomes, and missing `SENTRY_PROVISION_TOKEN`; it reports missing dogfood budget as a warning because the dispatcher currently treats absent budget rows as unbounded. No dogfood evidence was fabricated or projected.

Latest validation after the dogfood bootstrap audit: on 2026-06-28, the private focused audit lane passed with `7 passed`, focused ruff and ty checks passed, the target read-only audit exited `1` with expected blockers/warning, private `/Users/sidsharma/Syndai/.wt/codex-evalrank-w6-spine` `make check` passed, public `/Users/sidsharma/evalrank` `make check` passed with 223 Python tests plus 7 TypeScript SDK tests, and `git diff --check` passed in both repos.

Dogfood budget cap update: on 2026-06-28, the private Syndai worktree added `set-budget` to the existing Sentry dogfood provisioner and used it to set `siddmax/Syndai` repository `f568bbbb-fcb4-46c3-8fc2-04e530eeb805` to `monthly_usd_cap=50.00` in `syndai.coding_dogfood_budgets`. The follow-up target audit now reports `budget: monthly_usd_cap=50.00`; remaining dogfood blockers are missing `SENTRY_PROVISION_TOKEN`, missing Sentry project binding, and 0 terminal `sentry_auto` outcomes. This is private dispatcher cost-control config, not EvalRank evidence.

Latest validation after the dogfood budget cap: on 2026-06-28, the private focused provisioner lane passed with `10 passed`, focused ruff and ty checks passed, target dry-run/upsert/audit proof passed, private `/Users/sidsharma/Syndai/.wt/codex-evalrank-w6-spine` `make check` passed, public `/Users/sidsharma/evalrank` `make check` passed with 223 Python tests plus 7 TypeScript SDK tests, and `git diff --check` passed in both repos.

Dogfood audit exact-command update: on 2026-06-28, the private Syndai worktree updated the existing read-only Sentry dogfood audit so its `next:` block prints the exact `provision` command when the target repository id and webhook URL are known. The latest target audit for `eternex / SYNDAI_BACKEND` and `siddmax/Syndai` still exits `1` with expected blockers: missing `SENTRY_PROVISION_TOKEN`, missing Sentry binding, and 0 terminal `sentry_auto` outcomes. Chrome was opened to Sentry login for manual sign-in before token creation/provisioning can continue. This is operator readiness only, not EvalRank evidence.

Latest validation after the dogfood audit exact-command update: on 2026-06-28, the red regression first failed on placeholder output, private focused provisioner tests then passed with `11 passed`, focused ruff and ty checks passed, and the target read-only audit exited `1` with expected blockers while printing the exact provision command.

Latest validation after the W7 keyed quota enforcement slice: on 2026-06-28, private focused controller/usage tests passed with `10 passed`, the adjacent EvalRank unit/source lane passed with `160 passed`, focused ruff and ty checks passed, private migration-boundary checks passed, and earlier same-day private/public full local gates passed after the visible-quota source slice. After a final header-assertion update, the focused private quota/controller/usage lane passed again with `10 passed`, focused ruff and ty passed, and `git diff --check` passed in both repos. Rerun both full local gates before any push because the final header-assertion-only edit has not had a fresh full-suite rerun.

Handoff status refresh: on 2026-06-28, the public status/progress docs were refreshed for the next session. No product code or public contract changed. The handoff pins the active worktrees, schema boundary, promotion posture, and next work order: finish broader source-adapter coverage first, then hosted/staging W7 proof plus billing settlement, then W8/W9 surfaces. Public-safe summary: `docs/build-log/2026-06-28-handoff-status-refresh.md`.

Tool-registry source-edge update: on 2026-06-27, the private worktree broadened the tool-registry source adapter by preserving every upstream `syndai.tool_registry_capability_sources` edge in private EvalRank entity/result provenance and deriving `source_count` from those edges. Red/green regressions proved the adapter no longer trusts a stale count and the projection SQL now aggregates `source_edges`; the focused adapter/script/migration lane passed with `45 passed`, and focused ruff plus ty checks passed. Target DB proof projected 100 candidates for `tool-registry-capabilities-source-edges-2026-06-27`, verified source-edge arrays on 100 entities and 100 result rows with edge counts ranging `4..2375`, verified retrieved-source-url counts ranging `5..2376`, correctly refused a stale cache write, then materialized active cache `rec_02a413e747daad5a1a99ff53` from the fresh source-edge snapshot. This improves auditability and source-adapter breadth while keeping large source-edge details out of request-time reads.

Tool-registry entry coverage update: on 2026-06-27, the private worktree added a raw registry-entry source adapter over `syndai.tool_registry` and kept emitted rows honest as `metadata_tracking_not_performance`. Red/green regressions proved the projector, projection script, benchmark-catalog migration, and served-cache caveat; the focused materializer/adapter/script/migration lane passed with `63 passed`, and focused ruff plus ty checks passed. Target DB proof applied `2026_06_27_023_evalrank_tool_registry_entry_benchmark`, projected 200 candidates for `tool-registry-entries-2026-06-27` with 50 rows each for API, CLI, MCP, and skill source families, verified all 200 result rows carry metadata-tracking provenance, then materialized active cache `rec_ada04f92b9e9a89a6758dd75` with top row `trust_tier=tracking-only` and caveat `metadata_tracking_not_performance`. This improves source-adapter breadth and discovery coverage without pretending registry metadata is behavioral capability.

Budget-ledger update: on 2026-06-27, the private worktree added `evalrank.budget_ledger` and `src.features.evalrank.budget_ledger.reserve_budget(...)` as the hard reservation gate for future async expensive scoring, synthesis, model-swap, and GTM jobs. Red/green regressions proved the helper and migration contract; the broader focused EvalRank migration/projection/import lane passed with `50 passed`, and focused ruff, ty, and migration-boundary checks passed. Target DB proof applied `2026_06_27_024_evalrank_budget_ledger`, verified `evalrank.budget_ledger` and `evalrank_cron` update access, reserved `0.250000` against proof scope `proof:evalrank-budget-ledger-2026-06-27`, then returned `budget.ceiling_reached` for an over-cap `0.800000` attempt while leaving the row at `cap_usd=1.000000`, `reserved_usd=0.250000`, and `spent_usd=0.000000`. This is the correct long-term placement: cost control gates async enqueue, while live `recommend()` continues serving cached rankings without budget-check latency.

Source-lifecycle update: on 2026-06-27, the private worktree added append-only `evalrank.source_evidence_events` plus a coding-router lifecycle projection script. Red/green regressions proved deterministic event ids/content hashes, insert-only writer SQL, the migration contract, and the projection-script contract; the focused lifecycle lane passed with `5 passed`, and the broader focused migration/projection/import lane passed with `52 passed`. Focused ruff, ty, and `scripts/check_evalrank_migration_boundary.py` passed. Target DB proof applied `2026_06_27_025_evalrank_source_evidence_events`, verified `evalrank_cron` can insert but not update, verified the append-only trigger, and projected 25 active coding-router lifecycle events for `coding-router-active-9d629dd4`: 12 `observed`, 12 `promoted`, and 1 `snapshot_active`. This is the correct long-term placement for stale-removal, tombstone, retention, and dispute semantics: lifecycle facts append offline, while live `recommend()` keeps reading cached rankings.

Staleness lifecycle update: on 2026-06-28, the private worktree wired the existing coding-router staleness sweep into the append-only EvalRank source lifecycle ledger. The sweep now appends `stale_removed` for stale source evidence rows and `superseded` when a replacement active source snapshot exists. The all-stale path records row-level stale-removal facts without weakening the existing non-empty active snapshot invariant. Tombstones remain out until a real source-owned tombstone or retention signal exists; cache replacement is not treated as source tombstoning.

Source-owned tombstone hook update: on 2026-06-28, the private worktree added deterministic `tombstoned` event construction to the same append-only lifecycle module. Red/green regressions prove the hook requires explicit retention-policy metadata, rejects source-snapshot mismatches, and stays compatible with insert-only writer semantics. This does not create a live retention worker yet; it gives real source adapters the correct append-only path when retention/tombstone signals exist.

Latest validation after the source-owned tombstone hook: on 2026-06-28, public `/Users/sidsharma/evalrank` `make check` passed with 223 Python tests plus 7 TypeScript SDK tests. Private `/Users/sidsharma/Syndai/.wt/codex-evalrank-w6-spine` `make check` passed after formatting, lint, type checking, static security guards, EvalRank migration-boundary checks, repository audits, and documentation checks. Private focused lifecycle/migration/import coverage passed with `10 passed`; focused ruff and ty passed; public `tests.test_repo_docs` passed after the final build-log update; explicit trailing-whitespace checks and `git diff --check` passed in both repos.

Latest validation after the source-lifecycle slice: on 2026-06-27, public `/Users/sidsharma/evalrank` `make check` passed with 223 Python tests plus 7 TypeScript SDK tests. Private `/Users/sidsharma/Syndai/.wt/codex-evalrank-w6-spine` `make check` passed after formatting, lint, type checking, static security guards, EvalRank migration-boundary checks, repository audits, and documentation checks. Private focused lifecycle/migration/projection/docs lane passed with `67 passed`; focused ruff, ty, and `scripts/check_evalrank_migration_boundary.py` passed. Target DB proof for migration `2026_06_27_025_evalrank_source_evidence_events` passed.

Latest validation after the staleness-lifecycle slice: on 2026-06-28, the private focused staleness lifecycle lane passed with `9 passed`, the broader private EvalRank lifecycle/migration/projection lane passed with `56 passed`, focused ruff and ty checks passed for the touched lifecycle/sweep files, `scripts/check_evalrank_migration_boundary.py` passed, and Doppler-backed rollback integration proof for `tests/integration/features/coding/router/test_router_staleness_sweep_cli.py` passed with `3 passed`. Public `/Users/sidsharma/evalrank` `make check` passed with 223 Python tests plus 7 TypeScript SDK tests. Private `/Users/sidsharma/Syndai/.wt/codex-evalrank-w6-spine` `make check` passed after formatting, lint, type checking, static security guards, EvalRank migration-boundary checks, repository audits, and documentation checks.

Latest validation after the coding-validation source-adapter slice: on 2026-06-28, the private focused adapter/script/migration lane passed with `5 passed`, the expanded private source/materializer/migration lane passed with `99 passed`, focused ruff and ty checks passed, `scripts/check_evalrank_migration_boundary.py` passed, target DB projection wrote 3 stage/evidence/result rows, target materialization emitted `rec_aaab288a1d36c339084ab725`, public `/Users/sidsharma/evalrank` `make check` passed with 223 Python tests plus 7 TypeScript SDK tests, private `/Users/sidsharma/Syndai/.wt/codex-evalrank-w6-spine` `make check` passed, and `git diff --check` passed in both repos.

## Agent Completion Rule

To choose the next task, start at `Wave Coverage`, find the earliest wave whose next gate is not green, and choose the smallest slice that moves that gate without crossing the public/private boundary. After the public W6 reference materializer, private target-DB proof-of-spine, empirical-Bayes calibration posterior rows, expanded item-response rows, capability-index component responses with publishable filtered IRT, pooled multi-chain Bayesian IRT fit scope with SBC, Stage-2 rows, Stage-4 rows, expanded contamination-integrity rows, source-owned semantic text arrays, detached live refresh, safe promotion, current-schema Claude Opus 20/20 promotion, source-owned task-level golden outcome rows, risk-adjusted materialization, Stage-3 no-ledger guard, guarded materialize-time Stage-3 tie-break hook, Codex admission smoke, Codex full-bank active projection, candidate-ledger Sonnet calibration source, explicit pooled-calibration Stage-2 consumption, private ingestion-worker cache refresh, private tool-registry capability source adapter, private tool-registry entry coverage adapter, coding validation outcome projection/materialization, cache snapshot-lineage CAS, optional API-key auth for cached recommendation reads, recommend-called fact-table ingestion, worker-owned source scheduling, auxiliary source refresh, dogfood bootstrap audit, and dogfood budget cap, the next work is real Sentry dogfood provisioning plus W7 hosted/quota/billing surface parity over the verified worker-backed scorer/materializer spine. Keep Pi out of default cache/promotion until its full comparable behavior gate passes.

A step is complete only when the implementation has both contract evidence and behavior evidence appropriate to its surface. TDD is the preferred way to build non-trivial logic, but a passing unit test alone is not enough for routes, CLIs, MCP tools, UI, DB work, or hosted runtime behavior.

| Surface changed | Minimum completion evidence |
| --- | --- |
| Pure contract/parser/materializer logic | Red/green focused unit test, public fixtures updated if shape changes, then `make check`. |
| CLI behavior | Focused CLI test plus local command invocation for the changed command when practical. |
| SDK HTTP behavior | Focused SDK test against a local fake server, including success and Problem Details error paths. |
| MCP behavior | Manifest/list-tools test plus local `call_tool(...)` behavior test for success and error paths. |
| REST/API server implementation | Start the local server, `curl` the changed endpoint for success and error cases, then run contract tests. |
| Web UI | Start the local app, verify the changed flow with Playwright or equivalent browser automation, and capture failure-prone states such as loading, empty, error, and mobile layout when relevant. |
| DB/migrations | Run migrations against a local database or disposable project, verify schema/data invariants with SQL, and run rollback/replay checks when supported. |
| Hosted/private runtime | Exercise the staging/live path with real HTTP/tool calls plus logs/traces; do not count mocked local tests as a live gate. |
| Docs/status-only change | Focused docs drift test plus `make check`; no endpoint/browser proof is needed unless the doc claims a runnable surface. |

Every build log for a completed slice should name the commands or live checks that prove the step. If a surface cannot be locally exercised yet, the status must say it is a contract-only or synthetic-only slice and keep the wave/spec percentage below runtime coverage.

## Coverage Rubric

| Coverage | Meaning |
| --- | --- |
| 0% | Not implemented. |
| 10% | Public-safe plan/status only; no executable contract. |
| 25% | Storage-free contract/schema/fixture exists. |
| 40% | Public SDK/CLI/MCP/docs/checks consume the contract; no live runtime. |
| 60% | Runtime exists over synthetic or local private data. |
| 80% | Hosted or private data path works end to end; launch ops still incomplete. |
| 100% | Wave/spec gate is green with production-grade checks. |

| Lens | Coverage | What this means now |
| --- | ---: | --- |
| Public open-core repo | 58% | Contracts, schemas, SDK/CLI/MCP clients, fixtures, route contracts, docs, CI, and the storage-free public reference materializer are built; no hosted server, persistence runtime, or private scorer. |
| Full EvalRank product | 83% | Foundation, API contracts, public reference materializer, private target-DB W6 proof caches with real capability-search, capability-index, tool-registry capability metadata plus source-edge provenance, tool-registry entry metadata-tracking coverage across API/CLI/MCP/skill families, active coding-router benchmark, raw coding-router ledger projection inputs, live coding-validation outcome projection and cache proof, projection-only worker refresh of auxiliary tool-registry/coding-validation/coding-dogfood sources with empty dogfood skipped, a read-only dogfood bootstrap audit that identifies the missing Sentry binding/token/terminal outcomes without fabricating evidence, empirical-Bayes calibration posterior rows, 21 earlier agent-lane item-response rows across active evidence plus raw-ledger sources, 40 current task-level golden outcome rows for Claude Opus plus Codex, 26 candidate-ledger Sonnet task responses, 203 capability-index tool-lane criterion responses with publishable filtered IRT, a pooled multi-chain Bayesian 2PL IRT fit scope with publishable SBC, explicit pooled-calibration Stage-2 consumption with required-missing calibration guard, private ingestion-worker cache refresh after live non-dry eval pipeline runs, worker-owned private `pg_cron -> pgmq -> worker` source-refresh scheduling, source-snapshot-lineage CAS on the private recommendation cache, private cache-backed W7 recommendation read routes with bounded `recommend.called` event/span fields, persisted append-only `recommend.called` fact rows, keyed current-period usage summary backed by a private visible quota source, keyed quota enforcement with explicit `429` cached fallback, and optional `evalrank:recommendations:read` bearer-key auth, Stage-4 conformal-shortlist rows, guarded materialize-time Stage-3 tie-break behavior, expanded contamination-integrity checks with live exact-overlap provenance, source-owned semantic text arrays, detached live refresh, safe carry-forward promotion, current-schema Claude Opus 20/20 active rows, Codex GPT-5.5 19/20 active measured rows, candidate-only Sonnet 12/20 calibration rows, risk-adjusted materialized cache proof with Claude Opus ranked first and Codex second, benchmark-release metadata, model cutoff metadata, a hard private budget reservation gate for future async expensive scoring queues, append-only active/staleness/tombstone coding-router source lifecycle event construction, a contract-ready admission-eval projection, private benchmark catalog FK integrity, append-only private evidence enforcement, and private source-owned input snapshots with lineage FKs exist. The full multi-source data-plane evidence graph, broader R9-R36 integrity methods, full hosted surface parity, billing settlement, broader observability transport, GTM, and governance remain mostly private/incomplete. |
| W6 proof-of-spine | 100% | Public storage-free materializer emits deterministic materialized-cache ranking/abstention from provided public inputs, and the private Syndai worktree has DB-shaped cache/materializer code, tests, disposable PostgreSQL proof, target-DB fixture proof, real capability-search projection proof, capability-index projection proof, capability-index component item-response plus publishable filtered IRT proof, active coding-router benchmark evidence proof, raw coding-router ledger proof, empirical-Bayes calibration-row proof, item-response-row proof, synthetic IRT recovery proof, pooled IRT/SBC proof, Stage-2 scorer-row proof, Stage-4 conformal-shortlist proof, guarded materialize-time Stage-3 tie-break proof, expanded contamination-aware cache proof, source-owned text metadata proof, detached live golden-eval refresh proof, safe promotion proof, current-schema Claude Opus 20/20 active-row proof, Codex GPT-5.5 19/20 active-row proof, and rematerialized cache proof for agent and tool-lane snapshots. W6 is green; next work is W1-W5 breadth and W7+ product surfaces. |
| Public/private boundary safety | 70% | Boundary scanner, docs, package hygiene, secret/private-data guards, and porting router exist; branch protection and private-side runtime gates still need live verification when shipping. |

## Wave Coverage

| Wave | Full implementation coverage | Public repo coverage | Current status | Next gate |
| --- | ---: | ---: | --- | --- |
| W0 Foundation, frozen contracts, scaffolding | 65% | 80% | Public repo, package boundaries, CI, OpenAPI/contracts, and W0 exit packet exist; private DB bootstrap stays in Syndai. | Refresh branch protection/live repo settings before a release push. |
| W1 Data-plane core tables | 88% | 45% | Storage-free entity/evidence/result contracts are public; private W6 proof tables, grants, RLS, migration runner, capability-search input projection, admission-eval projection, capability-index projection plus component item responses, tool-registry capability projection with upstream source-fingerprint/per-source-edge preservation, raw tool-registry entry projection with metadata-tracking provenance, active coding-router benchmark projection, raw coding-router ledger projection, live coding-validation outcome projection, benchmark catalog/result FK integrity, append-only evidence enforcement, append-only active/staleness/tombstone coding-router source lifecycle event construction, scorer-row storage, contamination-check storage, item-response storage, IRT fit/item-parameter storage, recommendation-cache source-snapshot lineage, and source-owned active input snapshots with stage/evidence/result/scorer/calibration/item-response/IRT/cache lineage are green for the cache spine. The target DB has no resolved admission-eval rows yet, and the full multi-source L1 evidence ledger, live source-owned tombstone/retention workers, and production graph completeness remain private/incomplete. | Complete broader private evidence-ledger/source-adapter table slices beyond the current proof sources. |
| W2 Entity extensions, trust, security primitives | 10% | 15% | Public trust/security labels and boundary notes exist; scanners, guarded fetch, sanitizer wall, trust runtime, and auth/payment safety primitives are not public runtime. | Private adversarial security tests over the real primitives. |
| W3 IRT/math, scanners, API plumbing | 61% | 10% | Methodology/version/error contracts exist; the private worktree now has an empirical-Bayes calibration posterior contract, a real item-response ledger for `n_items = 1` observations across active coding-router evidence and raw-ledger sources, source-owned task-level golden outcome rows projected into `golden-eval-task-v1` item responses for Claude Opus and Codex, a candidate-ledger Sonnet calibration source, source-independent case item ids across active/candidate snapshots, a publishable 3-entity/9-item/27-binary-response agent-lane pooled IRT fit (`max_r_hat=1.003291`, `min_effective_sample_size=1167.966431`, no divergences, `sbc_publishable=true`, `sbc_rank_count=210`), explicit Stage-2 consumption of that pooled calibration source with missing-required-calibration guardrails, a source-specific capability-index component-response matrix with 29 entities and 203 responses, a publishable filtered capability-index IRT fit over 3 informative items (`max_r_hat=1.005729`, `min_effective_sample_size=563.65863`, no divergences), prior publishable pooled diagnostics over the broader eligible matrix, synthetic parameter-recovery evidence, deterministic temporal/source/fingerprint/semantic contamination-integrity checks, live exact-overlap provenance, private benchmark-release metadata, private model cutoff metadata, coding-router redacted semantic-text ingress, source-owned semantic text arrays, current-schema Claude/Codex rows with provenance text, and the first private hard budget reservation table/helper for async scoring enqueue. Auth plumbing, settlement, abuse limits, pagination, and correlation middleware are not implemented here. | Broaden API/auth plumbing and source-adapter coverage over the verified calibration/scoring path. |
| W4 Calibration, CAT, ingestion workers, retrieval stages | 37% | 20% | Stage-1 public `StageCandidate` shape exists, private aggregate calibration rows feed Stage-2, private item-response rows provide the response-matrix substrate, the candidate-ledger ingestion CLI accepts full non-dry golden snapshots as calibration-only evidence, the private tool-registry adapters add live tool-lane metadata sources behind the same projection writer, preserve per-upstream source edges, and now project balanced API/CLI/MCP/skill registry-entry coverage as tracking-only metadata, the private IRT batch path can emit `irt_2pl_numpyro` calibration rows when the matrix is eligible, Stage-2 can consume an explicit pooled calibration source without mixing candidate rows into active evidence, and the eval-pipeline worker now runs replayable non-dry cache refreshes over deterministic active/candidate/pooled snapshot ids plus projection-only auxiliary source snapshots from the durable private `pg_cron -> pgmq -> worker` source-refresh path. pgvector/RRF recall checks, CAT, adaptive item-selection policy, terminal dogfood source rows, and broader independent source-job lanes are private/not built here. | Produce terminal dogfood source rows and keep CAT behind calibrated item-bank gates. |
| W5 Live eval/ingestion, scorer stages 2/3/4, router | 84% | 10% | Public recommendation response envelope exists; private capability-index health/result projection plus 203 component responses and publishable filtered IRT, tool-registry capability metadata projection with 100 live candidates/results plus source-edge provenance, tool-registry entry metadata projection with 200 balanced API/CLI/MCP/skill candidates and tracking-only cache caveats, active coding-router benchmark evidence, raw coding-router ledger rows, live coding-validation outcome rows with auxiliary standardized-trust proof materialization, projection-only worker refresh over tool-registry capabilities, raw registry entries, coding-validation outcomes, and dogfood verified runs with source-empty dogfood skipped, read-only Sentry dogfood bootstrap audit with target proof of missing token/binding/terminal outcomes, target `siddmax/Syndai` dogfood budget cap set to `monthly_usd_cap=50.00`, task-level golden-eval case outcomes, 40 current task-level golden item responses across Claude Opus and Codex, 26 candidate-ledger Sonnet item responses, empirical-Bayes calibration posterior rows, a publishable 3-entity agent-lane pooled IRT/SBC fit, explicit pooled-calibration Stage-2 scorer consumption, required-missing calibration guardrails for uncalibrated active candidates, replayable worker cache refresh after live non-dry eval pipeline runs, durable worker-owned source scheduling through private `pg_cron -> pgmq -> worker` orchestration, cache-backed recommend-call event/span fields plus persisted `evalrank.recommend_events` facts, source-snapshot-lineage CAS plus a served-cache source guard protecting `autonomous-swe-agent` active cache swaps, append-only lifecycle events for active/stale/tombstoned coding-router source rows, Stage-4 conformal-shortlist rows, guarded materialize-time Stage-3 tie-break behavior, expanded contamination-integrity flags including semantic rows, live coding-router source/fingerprint provenance, tool-registry upstream fingerprint provenance, benchmark-release metadata, model cutoff metadata, redacted semantic-text ingress, source-owned semantic text arrays plus golden-eval snapshot plumbing, private golden-eval runner parity, private Pi runtime image refresh, full Pi runtime/provenance plus review-governance coverage, current-schema Claude Opus 20/20 active promotion, Codex GPT-5.5 19/20 full-bank active promotion, safe promotion carry-forward, a hard `budget_ledger` reserve-or-skip gate for future async expensive work, a coding dogfood verified-run adapter that is ready but currently source-empty, and restored risk-adjusted active cache `rec_230030790dc52449ed7625be` now feed or protect active caches for agent and tool-lane snapshots. Dispatcher integration, cluster router, independent auxiliary source job lanes, broader telemetry transport/analytics, settlement against actual cost, live source-owned retention workers, terminal dogfood source rows, and promotion-quality Pi full-bank behavior are not complete here. | Provision the real Sentry binding/token/webhook, then let existing dispatcher runs produce terminal dogfood source rows. |
| W6 Engine library and materializer | 100% | 35% | Public reference materializer emits deterministic `materialized-cache` recommendations or abstentions from supplied storage-free inputs. Private Syndai cache/materializer code, tests, disposable PostgreSQL proof, target-DB fixture proof, target-DB real capability-search projection proof, target-DB tool-registry capability projection proof with cache lineage, target-DB capability-index projection, component-response, and publishable filtered IRT proof, active coding-router benchmark evidence proof, raw coding-router ledger proof, calibration-row proof, item-response-row proof, synthetic IRT recovery proof, Stage-2 scorer-row proof, Stage-4 conformal-shortlist proof, guarded materialize-time Stage-3 tie-break proof, contamination-aware cache proof, source-owned semantic-text metadata proof, detached live refresh proof, validated promotion proof, source-owned task-level golden outcome projection for Claude and Codex, and rematerialized active cache proof now exist in a separate worktree. | Broaden refreshed active-row coverage before treating W7+ surfaces as product-ready. |
| W7 REST/MCP/CLI/keys/billing/vendor surfaces | 53% | 40% | Public route contracts and SDK/CLI/MCP clients exist, and the private worktree now has cache-backed `POST /v1/recommendations`, `POST /api/v1/recommendations`, and `GET /api/v1/recommend?use_case=...` read routes over the active materialized cache with focused route/telemetry tests, local live `curl` proof, fixed-query parity across public Python SDK, TypeScript SDK, CLI, and MCP, optional `evalrank:recommendations:read` bearer-key auth through Syndai's existing customer API-key control plane, keyed-only `GET /api/v1/evalrank/usage/current-period` usage aggregation over persisted `recommend.called` facts, visible quota state from private `evalrank.recommendation_quota_limits` rows, keyed quota enforcement returning `429` plus retry/quota headers and cached fallback when configured quotas are exhausted, private async billing settlement/readback rows dispatched by the existing coding webhook worker, target DB settlement migration/index/policy proof, rollback-only settlement enqueue proof, an explicit EvalRank Stripe meter setup profile, and private authenticated `GET /api/v1/evalrank/billing/readbacks` over customer-scoped settlement rows. Real billable keyed recommendation facts, hosted/staging proof, live Stripe dispatch/readback, receipts, and production-latency/load proof are not complete. | Hosted/staging W7 proof plus live billing settlement/readback once provider/account gates are cleared. |
| W8 Web, clients, observability transport, events | 14% | 5% | `NAVIGATION.md` covers API contracts only; the private worktree now persists cache-backed `recommend.called` events into append-only `evalrank.recommend_events`, exposes keyed current-period aggregate usage from those facts, records official OTel HTTP span attributes on cached recommendation reads, refreshes private daily SLI rollups over recommendation facts, schedules those rollups through durable `pg_cron -> pgmq -> worker` orchestration, and has a reusable local/staging load SLO smoke command. No public UI, event stream, deployed load proof, or hosted trace/export assertion exists. | Web renders a live verdict and broader telemetry flows to analytics/KPI tables. |
| W9 Metrics, self-improvement, governance, GTM fleet | 26% | 0% | No launch/governance/GTM automation is implemented in this public repo. Privately, WAQC weekly rollups, draft-PR-required regression facts, a local draft-PR payload artifact builder that can read private regression rows, is gated on DB-backed operator-affiliation completeness, can record sanitized draft-PR dispatch readback, and has an explicit account-gated GitHub provider dispatch mode that keeps dry-run JSON as the default, reads tokens from env, reuses the existing GitHub repository provider, and records provider-returned PR URLs. A pre-publication governance tripwire plus runnable metadata-only check command, an operator-affiliations completeness registry, a runnable affiliation-completeness check command, and a private GTM community-review queue that can fetch, dedupe, export, and explicitly deliver sanitized public Reddit/HN thread metadata to human-review channels also exist; live GitHub dispatch proof, live Slack/Resend proof, counsel signoff, advisory workflows, LLM reply drafts, and GTM agents are not complete. | Live GitHub draft-PR proof, advisory/legal gates, and human-gated GTM workflows green privately. |

## Spec Coverage

| Spec | Full implementation coverage | Public repo coverage | Public-safe status |
| --- | ---: | ---: | --- |
| Spec 00 Main | 20% | 35% | Thesis and invariants are reflected in public contracts, but the real evidence-ranked decision layer is not live. |
| Spec 01 Product | 10% | 15% | Public API/client surfaces exist; product workflows, hosted UX, and customer-facing loops are not implemented here. |
| Spec 02 Business | 5% | 5% | Business model remains private planning; public repo only carries open-core boundary and license posture. |
| Spec 03 Architecture | 33% | 45% | Public package/API boundaries, storage-free contracts, reference materializer boundary, private source-snapshot/scorer-row/contamination-check/item-response/IRT lineage, and multiple private source adapter projections are partially implemented; deploy topology and most private runtime architecture remain private/incomplete. |
| Spec 04 Engineering | 28% | 35% | CI, package hygiene, schemas, boundary checks, private EvalRank schema migrations, private projection scripts, the first private scorer-row runner, the first private contamination-check runner, and cache source-snapshot lineage/CAS are underway; most physical runtime/service implementation is absent from the public repo. |
| Spec 05 Data Methodology | 64% | 30% | Public methodology version, scoring-stage vocabulary, result/evidence contracts, storage-free reference materializer, active private agent benchmark projection, raw private router-ledger projection, private item-response rows across active evidence/raw-ledger/candidate-ledger sources plus capability-index component criteria, empirical-Bayes private calibration posterior rows, private pooled and capability-index NumPyro/JAX Bayesian 2PL paths with publishable diagnostics, publishable SBC, source-independent case item identities, synthetic recovery evidence, explicit pooled-calibration Stage-2 consumption with missing-required-calibration guardrails, private Stage-4 conformal-shortlist rows, guarded materialize-time Stage-3 tie-break behavior, deterministic temporal/source/fingerprint/semantic contamination-integrity checks, live coding-router source/fingerprint provenance, benchmark-release metadata, model cutoff metadata, redacted semantic-text ingress, source-owned semantic text arrays, refreshed promoted Claude/Codex full-bank rows with semantic provenance, candidate-only Sonnet full-bank calibration evidence, and tool-registry metadata-tracking rows that stay visibly non-performance evidence exist; CAT, refreshed live sanitized text metadata for every active row, and broader R9-R36 integrity methods remain private/incomplete. |
| Spec 06 Trust/Security/Auth/Payments | 8% | 15% | Public trust labels and secret/private-data guards exist; auth, payments, scanner runtime, and reputation systems remain private/not built. |
| Spec 07 Carve-Out/Integrate-Back | 45% | 60% | Public/private boundary, porting map, Apache-2.0 repo, and package split are strong; Syndai dogfood integration remains private. |
| Spec 08 Prior Art/Refinements | 15% | 20% | Used as design input; only public method notes and boundary choices are reflected in code/docs. |
| Spec 09 Ingestion/Seeding/Scoring Ops | 47% | 15% | Raw-entry/result/evidence shapes exist; private source-owned input snapshots now record projection state for capability-search, admission-eval, capability-index, tool-registry capabilities with source-edge provenance, raw tool-registry entries with balanced source-family coverage, active coding-router evidence, raw coding-router ledger feeds, live coding-validation outcome rows, and adapter-ready coding dogfood verified-run aggregates. Private item-response, capability-index component-response, calibration-row, IRT fit, Stage-2 scorer-row, expanded contamination-check, and coding-router source-lifecycle event runners write cache/scoring/lifecycle rows when eligible, including staleness-sweep `stale_removed`, replacement-snapshot `superseded`, and deterministic source-owned `tombstoned` event construction. Empty tool-registry and dogfood projections fail closed unless explicitly allowed, and validation-outcome projection now proves a non-empty live behavior source through materialization. The eval-pipeline worker now has a replayable non-dry cache refresh that rebuilds the active coding-router recommendation cache from deterministic active/candidate/pooled snapshot ids. The private budget ledger now enforces reserve-or-skip semantics before future expensive async scoring enqueue. Seeding queues, scoring queues, dispatcher integration, broader source workers, terminal dogfood source rows, live retention workers, and ops loops remain incomplete here. |
| Spec 10 Benchmark Unification/Integrity | 54% | 15% | Result-row and integrity flag shapes exist; private benchmark catalog seeds now include coding-router benchmark sources, capability-index criterion items, tool-registry metadata-coverage rows, tool-registry metadata-tracking rows, coding-validation pass-rate rows, benchmark-release metadata, model cutoff metadata for current coding-router entities, raw ledger projection, active and candidate-ledger item-response rows with source-result lineage, source-independent case item identities for comparable full-bank tasks, IRT fit/item-parameter storage including non-informative item exclusion diagnostics, empirical-Bayes calibration rows, explicit pooled-calibration source provenance in Stage-2 scorer rows, Stage-4 shortlist rows, temporal/source/fingerprint/semantic contamination-integrity rows, live exact-overlap provenance across existing adapters, upstream tool-registry fingerprint/source-edge/registry-entry provenance, source-owned coding-router semantic text metadata, append-only active/stale/tombstoned coding-router lifecycle events, and refreshed promoted Claude/Codex plus candidate Sonnet full-bank rows that prove semantic checks and item identity can be source-owned; refreshed sanitized text coverage for all carried active rows, live deprecation/tombstone state machines, and synthesis stay private/unbuilt. |
| Spec 11 Gaps/Reuse/Decisions | 20% | 30% | Public decisions and reuse boundaries are partially encoded in docs/CI; private runtime reuse work is not tracked as public implementation. |
| Spec 12 Auth/Onboarding/Tiering | 8% | 7% | Public Problem Details includes auth-related codes; the private worktree now maps the public read concept to `evalrank:recommendations:read` for cache-backed recommendation reads through Syndai's existing customer API-key control plane, exposes configured quota state from a private EvalRank quota source, and enforces exhausted keyed recommendation quotas with `429` plus cached fallback. Full onboarding, key mint UI/API, tier management, billing, and hosted auth remain private/incomplete. |
| Spec 13 Growth/GTM | 8% | 0% | Public repo has no GTM automation. Privately, the first pre-publication gate exists so future GTM dispatchers can fail closed before social/report side effects, and a private community-review queue can now fetch, dedupe, export, and explicitly deliver sanitized Reddit/HN thread metadata for human follow-up once provider accounts exist. |
| Spec 13a GTM Agent Buildbook | 8% | 0% | Public repo has no GTM agent fleet. Privately, the publication-governance checker is now an input gate future agents can call before human-gated dispatch, and the first listener queue/fetch/digest/delivery substrate exists without account setup, social posting, LLM drafting, or default provider side effects. |
| Spec 14 Legal/Compliance | 8% | 10% | Public `SECURITY.md`, license, notice, and no-sensitive-reporting guidance exist; privately, pre-publication checks now block unevidenced negative labels, security labels without CVD clearance, and undisclosed material connections. Counsel/compliance signoff remains private/incomplete. |
| Spec 15 Governance/Neutrality | 20% | 15% | Neutral public/private boundary and rank/payment separation are documented; privately, the W9 gate now blocks missing operator-COI disclosure, records sanitized governance decisions before future public side effects, exposes a runnable metadata-only governance check command, checks that current Syndai-operated agent candidates have private affiliation registry rows, and blocks draft-PR artifacts until that check passes. Full conflicts-page, advisory-board, RFC, dispute, and public governance operations are not complete. |
| Spec 16 UI/UX | 0% | 5% | No public UI routes or components exist. Privately, the Syndai web app now has an authenticated `/[locale]/evalrank` live-verdict page backed by a no-store BFF route to the cached recommendation endpoint and a signed-in home CTA to reach it; public UI routes and navigation docs remain absent by design. A v1 public marquee/SEO site design (landing, `/best/<use-case>`, live receipts, methodology) on the Instrument design system, bounded to public-tier verdict data, is now drafted; implementation pending. |
| Spec 17 API Reference | 47% | 47% | OpenAPI contracts and public clients exist for three routes; the private worktree now has cache-backed public-root `POST /v1/recommendations`, Syndai-prefix `POST /api/v1/recommendations`, internal/read `GET /api/v1/recommend`, keyed-only `GET /api/v1/evalrank/usage/current-period`, and private keyed `GET /api/v1/evalrank/billing/readbacks` routes over the active materialized cache/fact/quota/settlement tables with local live SDK/CLI/MCP parity proof, optional bearer-key read auth, keyed over-quota `429` fallback behavior, and customer-scoped settlement readback. No public receipt contract or full hosted/staging billing parity surface exists. |
| Spec 18 Metrics/KPI/Events | 22% | 5% | Some public identifiers and timestamps exist; the private cached recommend route now builds `recommend.called` fields from the returned payload with bounded top-k ids and full ranked counts, persists successful events into append-only private `evalrank.recommend_events`, exposes current-period keyed usage aggregates plus visible quota state from private EvalRank tables, blocks exhausted keyed reads before writing another successful usage fact, derives daily SLI rollups, derives/schedules weekly WAQC north-star rollups while excluding first-party dogfood from headline adoption, and records private WAQC regression facts only on real week-over-week drops. Broader event transport, retention cohorts, dashboards, and governance analytics are not implemented here. |
| Spec 19 Financial/Open Source | 34% | 38% | Open-source posture, license, public package boundaries, and no-private-data rules exist; private cost-control now has a first hard budget reservation table/helper, customer-visible usage aggregation over recommendation facts, a private quota-limit source for visible remaining quota, keyed quota denial on exhausted configured limits, async billing settlement/readback rows dispatched outside the ranking request path, an authenticated private billing-readback route, an explicit EvalRank Stripe meter setup profile, and target DB migration/rollback enqueue proof, while real billable facts, live Stripe dispatch/readback, pricing execution, and receipts stay private/incomplete. |
| Spec 20 Observability/Telemetry/Self-Improvement | 15% | 6% | Public request/error fields exist; the private cached recommend route now attaches bounded EvalRank recommendation attributes to an active span when present, persists successful read facts into `evalrank.recommend_events`, exposes keyed current-period usage aggregates, refreshes daily SLI and weekly WAQC rollups, records human-gated WAQC regression facts, renders the first authenticated live-verdict web proof over the cached path, and has a private pre-publication governance audit substrate plus account-gated GitHub draft-PR dispatch wrapper for follow-up workflows. Live provider proof, automatic branch/commit creation, and broader analytics transport are not implemented here. |
| Spec 21 Design System/I18n | 0% | 0% | No public UI/design-system implementation exists. A v1 implementation design (port the `tokens.css` Instrument system, the buildable component subset, and the 24-locale i18n surface) is now drafted; implementation pending. |
| Spec 22 Methodology Hardening | 64% | 22% | Public schema hooks and reference behavior exist for methodology version, result rows, evidence, confidence intervals, abstention, stage ordering, deterministic materialization, private coding-router benchmark result projection, private tool-registry metadata-coverage projection with explicit non-performance score basis and source-edge provenance, private active/raw/candidate item-response-row projection, private capability-index component-response projection with publishable filtered IRT diagnostics, private empirical-Bayes calibration-row consumption, private pooled Bayesian 2PL IRT path with publishable multi-chain diagnostics and SBC, private candidate-ledger calibration source that stays out of active route evidence, explicit pooled-calibration Stage-2 consumption with missing-required-calibration guardrails, private Stage-4 conformal-shortlist consumption, Stage-3 no-ledger enforcement, guarded materialize-time Stage-3 tie-break behavior, cache source-snapshot lineage/CAS, append-only source lifecycle events including staleness removals and source-owned tombstone construction, expanded contamination caveats, cross-adapter live exact-overlap provenance, benchmark-release metadata, model cutoff metadata, redacted semantic-text provenance ingress, source-owned text metadata plumbing, and detached live refresh proof that semantic checks stay unknown unless retrieved text is actually present; broader R9-R36 integrity methods are not complete. |
| Frame/video layer | 0% | 0% | Not implemented in this repo. |

## Next Vertical Slice

Stop spending default cycles on envelope hardening. The public-safe W6 reference materializer now proves the `EvaluationRequest -> CandidateSet/StageCandidate -> EvidenceSet/ResultRow/Exclusion -> Recommendation/Abstention` path over supplied storage-free inputs, and the private Syndai worktree now has target-DB W6 proof caches fed by real capability-search rows, the broader capability-index projection and component item responses with publishable filtered IRT, the tool-registry capability metadata projection, active coding-router benchmark evidence, raw coding-router ledger rows, empirical-Bayes calibration posterior rows, item-response rows across active evidence, raw-ledger, and candidate-ledger sources, source-owned task-level golden outcome rows, a pooled Bayesian 2PL IRT batch path with publishable multi-chain diagnostics and SBC, explicit pooled-calibration Stage-2 consumption, a replayable non-dry ingestion-worker cache refresh, cache-backed `POST /v1/recommendations`, `POST /api/v1/recommendations`, and `GET /api/v1/recommend` read routes with local live public-client parity proof plus optional `evalrank:recommendations:read` bearer-key auth, a private authenticated web live-verdict BFF/page over the cached recommendation route, bounded `recommend.called` event/span fields, append-only `evalrank.recommend_events` fact persistence, keyed current-period usage aggregation with visible quota state from `evalrank.recommendation_quota_limits`, keyed quota enforcement with explicit cached-fallback `429` responses, private async billing settlement/readback dispatched by the existing coding webhook worker, a private authenticated billing-readback route over customer-scoped settlement rows, private daily recommendation SLI rollups, scheduled private weekly WAQC north-star rollups, deterministic private WAQC regression-event facts, private pre-publication governance tripwires plus runnable metadata-only check command, a DB-backed affiliation-completeness gate on local draft-PR artifacts, DB-backed regression-row loading for draft-PR payloads, sanitized draft-PR dispatch readback recording, account-gated GitHub draft-PR provider dispatch over the existing repository provider, a private GTM community-review queue/fetcher/digest exporter/delivery wrapper for sanitized Reddit/HN thread metadata, and a private operator-affiliations completeness registry for current Syndai-operated agent candidates, source-snapshot-lineage CAS for active cache swaps, append-only source lifecycle events for active, stale-removed, and source-owned tombstoned coding-router rows, Stage-4 conformal-shortlist rows, guarded materialize-time Stage-3 tie-break behavior, expanded temporal/source/fingerprint/semantic contamination-integrity checks, live coding-router exact-overlap provenance, benchmark-release metadata, model cutoff metadata, redacted semantic-text ingress, source-owned semantic text arrays, detached live refresh, safe promotion carry-forward, current-schema Claude Opus 20/20 active promotion, private Pi runtime image refresh, full Pi runtime/provenance plus review-governance coverage, Codex 19/20 full-bank active projection, candidate-only Sonnet 12/20 calibration projection, and risk-adjusted active cache proof for `autonomous-swe-agent`. Admission-eval projection is ready but currently has 0 target rows; benchmark ids are cataloged/FK-enforced, evidence rows are append-only, and source-owned input snapshots bind stage/evidence/result/scorer/calibration/item-response/IRT/cache rows to a projection lineage. The next useful slice is hosted/staging W7 proof plus live billing settlement/readback once provider/account gates are cleared; remaining W9 work is live GitHub dispatch proof, live Resend/Slack community digest proof, and human-gated GTM workflows after account setup exists.

## Next-Session Handoff

- Public repo: continue from `/Users/sidsharma/evalrank` on branch `codex/full-spec-status-reporting`. Keep this repo product-neutral and storage-free: contracts, schemas, SDK/CLI/MCP surfaces, examples, docs, and boundary checks only.
- Private repo: continue Syndai work from `/Users/sidsharma/Syndai/.wt/codex-evalrank-w6-spine`, not the Syndai `main` checkout. Do not push either repo unless the owner explicitly asks in the new session.
- Database boundary: private EvalRank persistence stays in the dedicated private `evalrank` schema. Syndai shared customer/API-key/control-plane tables stay in `syndai` because they are shared auth infrastructure. Private Syndai source adapters may read `syndai.*` inputs during incubation, but derived EvalRank rows, caches, catalog rows, grants, RLS policies, and migrations belong in `evalrank`.
- Current default evidence lane: Claude Opus is promotion-quality at 20/20; Codex GPT-5.5 is active measured evidence at 19/20; Pi is wired but quarantined from default cache/promotion until its full comparable behavior gate passes. Site-clone work is only a benchmark category inside the golden eval, not the product itself.
- Next work order: see the `Go-Live Tracker` section. The disclosure access-tier moat and Model A overage billing (test/live key isolation, opt-in self-capped overage, subscription guard) are now code-complete and committed on `codex/evalrank-w6-spine`; the immediate next steps are applying EvalRank migrations to the live DB, deploying the branch to `syndai-prod`, issuing a keyed test customer, and running the hosted smoke + billing proof. After that: the go-live billing flip (decisions §2.17), landing #3 (marquee website/SEO + public docs), and the still-open Sentry dogfood/account-gated GTM work. Design drafts now exist for landing #3: a v1 public marquee/SEO web site (Instrument design system, 24 locales, bounded to public-tier verdict data) plus a companion public-API expansion (add `ci95` + cost/latency/lane to the ranked payload; public `GET /v1/use-cases`, entity, compare, and receipt routes) that unblocks the trade-off/entity/compare surfaces, sequenced smallest-unblocking-slice first.
- Verification handoff: rerun the smallest focused lane for any touched slice first, then `make check` in public and private before a push. This session changed private code (EvalRank moat + Model A billing); the final private sweep was `220 passed` across the EvalRank and coding-Stripe lanes with focused ruff, ty, and the EvalRank migration-boundary check green. Public docs changes here should pass `python3 -m unittest tests.test_repo_docs`, public `make check`, and `git diff --check`.

## Go-Live Tracker

Landing #1 (disclosure moat) and landing #2 (Model A overage billing) are code-complete and committed in the private worktree on branch `codex/evalrank-w6-spine`; unit-verified with mocked Stripe/DB (`220 passed`), not yet deployed. EvalRank bills test-mode during incubation.

Done this session (2026-06-29):
- Disclosure access-tier gate: anonymous callers get `404` on `api`-tier use cases, full catalog for keyed callers; marquee categories stay public.
- Pricing/disclosure decisions pinned: 1¢ overage, marquee-public vs API-only tiers, hard-cap-default + opt-in self-capped overage.
- Test-mode Stripe wired (meter/product/price + `STRIPE_TEST_KEY` in dev and prod); EvalRank bills test-mode in prod during incubation.
- Model A billing: test/live key isolation, opt-in self-capped overage, billable-only settlement filter, stateless subscription guard.
- Preflight now covers EvalRank raw-SQL migrations.

Next, in order, to a proper hosted smoke test:
1. Apply EvalRank migrations to the live DB before deploy: `cd backend && doppler run -- uv run python scripts/apply_evalrank_migrations.py` (additive: new table + columns; preflight reminds on every EvalRank migration change).
2. Deploy the branch to `syndai-prod` (merge to the deploy branch, CI `flyctl deploy`) so `api.syndai.ai` serves the new gate + billing code. Gated on explicit owner instruction plus pre-landing review.
3. Issue a keyed customer with the `evalrank:recommendations:read` scope via the Syndai customer admin control plane; set it as `EVALRANK_API_KEY` for the keyed smoke checks.
4. Run the hosted smoke + load proof against `https://api.syndai.ai` with `--require-non-local-base-url`; verify anonymous marquee → 200 and anonymous `api`-tier → 404.
5. Billing proof (test mode): opt a test customer into overage, drive past quota, confirm one settlement row + one 1¢ meter event and the cap hard-stop.

Later / separate:
- Go-live billing flip (decisions §2.17) when ready to charge real money: remove `STRIPE_TEST_KEY` from prod, create the live meter/price, set the live `STRIPE_EVALRANK_PRICE_ID`.
- Per-customer overage config: set `overage_enabled` + `overage_monthly_cap_usd` for opted-in customers.
- Landing #3: public marquee website/SEO + public free-vs-paid docs (design review when started).
- Open question: whether the public `UseCase` contract needs `access_tier` (kept private for now).
- Prod Doppler GitHub-Actions sync is at the 100-secret cap; audited removable candidates exist if headroom is needed.

Pre-landing review (2026-06-29) — verdict "with fixes", no Critical. Fixed: a test now pins the anon-gate 404 as byte-identical to the unknown-use_case 404 (enumeration defense). Deferred:
- Before the first LIVE charge: confirm Stripe `/subscriptions?price=` reuse semantics on the pinned API version. It mirrors the production outcome-billing subscription lookup, so it's low risk, but verify to rule out a duplicate-subscription edge case (extra recurring invoice).
- Decide: a bearer token lacking `evalrank:recommendations:read` returns 401 even for a public marquee use case (worse-than-anonymous). This is pre-existing optional-API-key auth behavior, not introduced by the gate — confirm intended, or fall back to anonymous on auth failure for `public`-tier use cases.

## Built

- Public Apache-2.0 repository scaffold.
- Public package boundaries for core, MCP, CLI, Python SDK, and TypeScript SDK.
- Root and scoped `AGENTS.md` guidance.
- `CLAUDE.md` shim to `@AGENTS.md`.
- Public boundary checker for private imports, disallowed coupling, excluded method markers, and missing package license/notice files.
- Public boundary checker guards for secret files, high-signal secret values, and held-out/private data paths.
- Public Python package metadata drift guard for package names, versions, licenses, Python floor, dependency edges, and CLI entrypoint.
- Core Python capability fingerprint, raw entry, request, candidate set, stage candidate, evidence item, result row, use-case catalog, scoring stage catalog, ranking group, evidence set, exclusion, `the_call`, abstention, and recommendation contracts in `packages/core`.
- Public reference materializer in `packages/core` that emits deterministic `materialized-cache` recommendations or public abstentions from provided storage-free request, candidate, Stage-1, evidence, result, and exclusion inputs.
- Private Syndai W6 real-input projection path that feeds capability-search results into the private `evalrank` cache spine without adding a second ranker; target DB proof emitted a 2-candidate methodology-stamped cache row with no fixture caveat.
- Private Syndai admission-eval projection path that maps deterministic capability admission eval scorecards into the same private `evalrank` input rows; current target DB smoke found 0 resolved source rows.
- Private Syndai benchmark catalog integrity migration with seeded W6/projection benchmark ids and a validated FK from private `evalrank.result_rows`.
- Private Syndai append-only evidence enforcement on `evalrank.evidence_items`, with projection reruns using idempotent insert/no-op semantics instead of evidence mutation.
- Private Syndai source-owned input snapshot catalog with one active snapshot per source/use case, stage/evidence/result lineage FKs, and target DB proof that capability-search and admission-eval snapshots stay independently active.
- Private Syndai append-only source lifecycle event ledger for active, stale-removed, and source-owned tombstoned coding-router rows, with deterministic event ids/content hashes, insert-only writes, target DB proof for `snapshot_active`, `observed`, and `promoted` events, rollback integration proof for `stale_removed` plus replacement-snapshot `superseded` events, and unit proof for retention-signal `tombstoned` construction.
- Private Syndai capability-index projection path that maps 29 enabled normalized capability-index rows into EvalRank entities, evidence, result rows, a source snapshot, and a 29-candidate target-DB materialized cache row with no fixture caveat.
- Private Syndai coding-router evidence projection path that maps the active approved coding-router evidence snapshot into EvalRank agent entities, benchmark result rows, a source snapshot, and a 2-candidate target-DB `autonomous-swe-agent` materialized cache row.
- Private Syndai raw coding-router ledger projection path that maps approved router benchmark rows outside the active snapshot into a separate EvalRank source snapshot with 2 stage rows, 2 evidence rows, and 6 result rows while leaving the active cache unchanged.
- Private Syndai Stage-2 scorer-row layer that stores fixed-parameter `stage2_evidence_rerank` rows in the private `evalrank` schema and rematerializes the active `autonomous-swe-agent` cache with a `stage2_calibrated` score component.
- Private Syndai Stage-4 conformal-shortlist layer that stores `stage4_conformal_shortlist` rows in the private `evalrank` scorer-row table and rematerializes the active cache with `stage4_conformal_shortlist` score components.
- Private Syndai Stage-3 no-ledger guard that rejects persisted `stage3_llm_tiebreak` scorer rows and keeps LLM tie-breaks materialize-time-only.
- Private Syndai guarded Stage-3 materialize-time tie-break hook that accepts only validated adjudication artifacts for near-tied, clean Stage-4 candidates, never writes scorer rows, and leaves the current active cache unchanged when rows are not eligible.
- Private Syndai contamination-check layer that stores deterministic temporal-cliff, benchmark-source-overlap, and benchmark-fingerprint-overlap rows in the private `evalrank` schema, feeds non-clean flags into Stage-2 scorer rows, and rematerializes the active `autonomous-swe-agent` cache with `potential_contamination` caveats instead of silently treating missing metadata as clean.
- Private Syndai integrity provenance layer that attaches stable private source identifiers and fingerprints to existing source-adapter result provenance, allowing exact source/fingerprint contamination rows to become clean while temporal rows remain unknown until model cutoff metadata exists.
- Private Syndai semantic-text provenance ingress that redacts optional benchmark/retrieved text fields from coding-router source rows and lets semantic contamination checks become clean/flagged when real sanitized text is present.
- Private Syndai source-owned semantic text metadata that stores redacted `sanitized_benchmark_texts` and `sanitized_retrieved_texts` on the `syndai` source table, threads private golden-eval snapshots into those arrays, and keeps derived contamination/result/cache rows in the private `evalrank` schema; refreshed promoted rows can move semantic checks to clean, while carried active rows without retrieved text remain semantic `unknown`.
- Private Syndai golden-eval runner adapter parity that accepts the live executor dispatch keys, exercises the active Pi pair through the eval-pipeline subprocess dry-run path, proves live partial Pi probes now import the SDK governor and emit retrieved text, and records a post-site-clone-contract full 20-case Pi run with 20/20 retrieved text, review-only at 5/5, site-clone at 2/5, coding-repair at 5/10, and 12/20 overall passed, so current Pi evidence is improved but still not promotion-ready.
- Private Syndai current-schema Claude Opus golden-eval proof that reached 20/20 live pass/governance coverage for `claude_code / claude-opus-4-8`, populated benchmark/retrieved text for all 20 cases, promoted three active rows through safe carry-forward promotion, and rematerialized the active cache with Claude Opus ranked first.
- Private Syndai task-level golden outcome layer that stores source-owned `golden_eval_case_results`, projects case-level `golden-eval-task-v1` result rows plus stable case-id item responses, keeps aggregate golden rows as summaries with real case counts, and preserves the correct IRT skip until multiple entities have comparable task-level outcomes.
- Private Syndai Codex admission-smoke fix that runs `codex_cli / gpt-5.5` through explicit stdin prompting, eval-only local `auth.json` sandbox provisioning, and Syndai outer-sandbox execution, passing `off_by_one_python` as one selected task.
- Private Syndai Codex full-bank active projection that ingests `codex_cli / gpt-5.5` 19/20 live golden evidence, promotes measured rows into the active router snapshot, projects 20 Codex task outcomes, rematerializes the active `autonomous-swe-agent` cache with Claude Opus first and Codex second, and initially preserves the then-correct sparse-matrix IRT skip before the candidate-ledger calibration source adds the third comparable entity.
- Private Syndai candidate-ledger calibration source that ingests the full-bank `claude_code / claude-sonnet-4-6` 12/20 baseline as candidate-only evidence, projects 26 case item responses with source-independent task item ids, and fits a publishable pooled agent-lane IRT/SBC matrix over active plus candidate snapshots without changing active routing.
- Private Syndai pooled-calibration Stage-2 consumption that reads calibration rows from an explicitly named pooled source while keeping active evidence scoped to the active source snapshot, records calibration source provenance in scorer metadata, and gives uncalibrated active candidates `required_calibration_missing_v1` score/confidence `0.0` so they are not rewarded by fixed fallback.
- Private Syndai golden-eval detached transaction runner that keeps long live adapter subprocesses outside DB sessions, writes successful candidate snapshots through short transactions, and prevents Supabase idle-in-transaction failures during the weekly refresh path.
- Private Syndai detached live golden-eval refresh proof that wrote six candidate rows, promoted only the validated Claude review-only row into active snapshot `baebe224-dc7a-4f93-b7fc-6b487ce9bad6`, rebuilt projection/contamination/scorer rows, and rematerialized cache `rec_d03e376c9e516ffdf3d6fce5` with semantic rows staying clean only where retrieved text exists.
- Private Syndai cached recommend API read surface that serves `POST /v1/recommendations`, `POST /api/v1/recommendations`, and `GET /api/v1/recommend?use_case=...` from the active private `evalrank.recommendation_cache` row, returns the materialized recommendation payload unchanged, keeps anonymous reads anonymous, resolves bearer-key reads through `evalrank:recommendations:read`, and emits `recommend.called` event/span fields from the same payload.
- Private Syndai tool-registry capability source adapter that reads Syndai-owned tool-registry metadata, writes derived metadata-coverage candidates/results into the private `evalrank` schema, preserves upstream source fingerprints in provenance, refuses empty projections by default, and materializes a source-lineage-checked cache row.
- Private Syndai benchmark-release metadata migration that backfills `benchmark_released_at` for existing private benchmark catalog rows while keeping temporal contamination rows `unknown` until canonical model-training-cutoff metadata exists.
- Private Syndai model cutoff metadata layer that attaches provider-sourced cutoffs to current coding-router entities and moves temporal contamination rows from missing-metadata unknowns to explicit temporal-cliff flags.
- Private Syndai calibration-row layer that stores empirical-Bayes aggregate posterior rows in the private `evalrank` schema, feeds Stage-2 posterior score/confidence/CI values, and rematerializes the active `autonomous-swe-agent` cache through the same public-safe materialized response shape.
- Private Syndai item-response layer that stores active coding-router and raw-ledger `n_items = 1` result rows as deterministic binary/fractional item observations in the private `evalrank` schema, rejects aggregate rows, and adds FK-covering indexes for the response-matrix substrate.
- Private Syndai capability-index component-response layer that decomposes aggregate capability-index score components into normalized criterion item responses with response weights, adding a 29-entity/203-response tool-lane matrix while preserving the generic aggregate-row guard.
- Private Syndai IRT batch layer that filters non-informative binary items, fits a NumPyro/JAX Bayesian 2PL model over eligible binary item-response rows, stores private fit/item-parameter rows, and refuses sparse or non-publishable live matrices instead of publishing fake certainty.
- Private Syndai capability-index publishable IRT proof that fits the broader 29-entity tool-lane matrix over 3 informative criterion items with no divergences and publishable R-hat/ESS diagnostics.
- Private Syndai pooled IRT fit scope that fits the active use-case response matrix across source snapshots, stores pooled fit/item-parameter/calibration rows, and records publishable multi-chain plus SBC diagnostics.
- Public core fixture factory for canonical capability fingerprint, raw entry, request, candidate set, stage candidate, evidence item, Problem Details, result row, use-case catalog, scoring stage catalog, ranking group, evidence set, exclusion, and recommendation payloads with public abstention fields, with synthetic request use cases aligned to the public catalog.
- Shared public fixture-kind dispatch in core, reused by CLI and MCP fixture adapters.
- Public JSON Schemas for capability fingerprints, raw entries, evaluation requests, candidate sets, stage candidates, result rows, use-case catalogs, scoring stage catalogs, evidence sets, exclusions, ranked entities, recommendations with closed ranking groups and public abstention objects, evidence items, and retry-aware RFC 9457 Problem Details.
- Public OpenAPI 3.1.1 contract for `GET /v1/use-cases`, `GET /v1/scoring-stages`, and `POST /v1/recommendations`, including reusable Problem Details responses for malformed requests, validation errors, rate limits, temporary unavailability, and upstream timeouts.
- Public retry-aware Problem Details extensions: `code`, `retriable`, `retry_after`, `field`, `request_id`, and `doc_url`.
- Public Problem Details `doc_url` HTTP(S) hardening.
- Pinned public `methodology_version` format: calendar-valid `YYYY-MM-DD.SEQ.slug`.
- Public generated hash schema drift guard.
- Python SDK package metadata, public core contract re-exports, and dependency-free `EvalRankClient` behavior for `GET /v1/use-cases`, `GET /v1/scoring-stages`, and `POST /v1/recommendations`.
- TypeScript SDK package metadata, mirrored public contract types/constants, and dependency-free native `fetch` `EvalRankClient` behavior for `GET /v1/use-cases`, `GET /v1/scoring-stages`, and `POST /v1/recommendations`.
- Python and TypeScript SDK clients strictly parse `Retry-After` as non-negative integer seconds and treat malformed retry headers as absent while preserving Problem Details errors.
- SDK README drift checks for the Python and TypeScript public surfaces.
- CLI package metadata, deterministic public fixture command, explicit public `use-cases` and `scoring-stages` metadata commands, and explicit public `recommend` command for `POST /v1/recommendations`.
- MCP package metadata, deterministic public fixture adapter, explicit public `evalrank.use_cases` and `evalrank.scoring_stages` metadata tools, and explicit public `evalrank.recommend` tool for `POST /v1/recommendations`.
- MCP route tool input schemas now advertise non-empty HTTP(S) `base_url` values for public API calls.
- MCP recommendation tool input schema now advertises the public `EvaluationRequest` payload shape.
- Public scoring-stage vocabulary and catalog contract, use-case taxonomy method, and method-boundary notes.
- Runnable public fixture bundle example, including the scoring stage catalog.
- Schema drift tests for core payload keys and public enum constants.
- Exact package README metadata drift guard for Python package manifests and the TypeScript SDK manifest.
- Exact CLI and MCP README drift checks for public fixture commands, route commands, fixture kinds, and tool names.
- Exact schema README drift check for public schema and OpenAPI filenames.
- Exact methods README drift check for public method-note filenames.
- Exact repo structure drift check for public top-level directories and package directories.
- Exact status build-log index drift check.
- Exact porting workstream/status drift check.
- Exact public MCP docs boundary drift check.
- Exact TESTS abstention terminology drift check.
- Exact `CLAUDE.md` shim drift check for the required `@AGENTS.md` reference.
- Scoped `AGENTS.md` coverage drift check for public work areas.
- Ranked entity `axes.evidence` schema and TypeScript type hardening.
- Scoring-stage catalog schema uniqueness drift guard.
- Scoring-stage catalog ordinal contiguity hardening.
- Recommendation `ranked` schema uniqueness hardening.
- Recommendation `RankingGroup.ranked` schema uniqueness hardening.
- Recommendation group uniqueness hardening.
- Recommendation exclusion uniqueness hardening.
- Recommendation alias exact schema pattern drift guard.
- Recommendation abstention envelope consistency hardening.
- Recommendation abstention-as-empty-single-scale hardening.
- Recommendation `shortlist_depth` count consistency hardening.
- Recommendation ranked-row rank contiguity/order hardening.
- Ranked entity freshness date format hardening.
- Public temporal field format hardening.
- Result row public source URL hardening.
- Public confidence interval schema drift guard.
- TypeScript SDK non-empty array helper type for schema `minItems: 1` public arrays.
- TypeScript SDK `UseCase` discriminated union for public ranked and veto-overlay branches.
- TypeScript SDK `TheCall` discriminated union for public `recommend` and `abstain` branches.
- TypeScript SDK `Recommendation` discriminated union for public `single-scale` and `kind-grouped` branches.
- TypeScript SDK `RecommendationCallState` helper type now includes the public abstention branch.
- Python core `CandidateSet` and `EvidenceSet` now reject mutable list-backed sequence inputs before serialization.
- Tests for core contracts, schema-contract drift, repo docs, and public boundary rules.
- Public progress tracker and repo structure map.
- Public route navigation map in `NAVIGATION.md`.
- Public porting map for deciding what moves from Syndai/private workstreams into this repo.
- Earlier public/private porting audit confirming the then-current private Syndai dirty worktree had Memphant spec edits plus two Memphant plan files and no EvalRank public-port candidate.
- Public/private source scan classifying the current Syndai EvalRank spec, build-readiness, migration, and doc-validation surfaces without copying raw private text.
- Public/private source inventory refresh documenting current private EvalRank specs, build plans, proof assets, backend migration assets, GitHub security settings, and next public port slices without copying raw private text.
- GitHub public repo security metadata snapshot confirming public visibility, secret scanning, push protection, and Dependabot security updates are enabled.
- Public-repo safety guidance in `SECURITY.md` for private disclosure, no public secret reports, immediate rotation on exposure, and coordinated remediation if sensitive data reaches Git history.
- Direct `main` push workflow during scratch-build phase; branch protection is currently removed.
- `make check` local gate shared with CI.
- W0 core repository snapshot in `docs/build-log/2026-06-25-w0-core-repo-snapshot.md`.
- W0 public exit packet in `docs/build-log/2026-06-25-w0-public-exit.md`.
- Public W1 entity/evidence contract plan in `docs/superpowers/plans/2026-06-25-entity-evidence-contracts.md`.
- Public porting workstream sync in `docs/build-log/2026-06-25-porting-workstream-sync.md`.
- W1 entity/evidence contract build log in `docs/build-log/2026-06-25-w1-entity-evidence-contracts.md`.
- Python SDK re-export build log in `docs/build-log/2026-06-25-python-sdk-reexports.md`.
- CLI fixture command build log in `docs/build-log/2026-06-25-cli-fixtures.md`.
- MCP fixture adapter build log in `docs/build-log/2026-06-25-mcp-fixtures.md`.
- Public scoring-stage build log in `docs/build-log/2026-06-25-scoring-stages.md`.
- Public progress and porting audit in `docs/build-log/2026-06-25-public-progress-and-porting-audit.md`.
- TypeScript SDK public type surface build log in `docs/build-log/2026-06-25-typescript-sdk-types.md`.
- Runnable public fixture example build log in `docs/build-log/2026-06-25-public-fixture-example.md`.
- Evaluation request contract build log in `docs/build-log/2026-06-25-evaluation-request-contract.md`.
- Methodology version format build log in `docs/build-log/2026-06-25-methodology-version-format.md`.
- Methodology version calendar-date hardening build log in `docs/build-log/2026-06-26-methodology-version-calendar-hardening.md`.
- Capability fingerprint contract build log in `docs/build-log/2026-06-25-capability-fingerprint-contract.md`.
- Public progress and porting router refresh in `docs/build-log/2026-06-25-progress-and-porting-router.md`.
- Recommendation join aliases build log in `docs/build-log/2026-06-25-recommendation-join-aliases.md`.
- Public port-over status refresh in `docs/build-log/2026-06-25-public-port-over-status.md`.
- Raw entry contract build log in `docs/build-log/2026-06-25-raw-entry-contract.md`.
- Structured `the_call` contract build log in `docs/build-log/2026-06-25-the-call-contract.md`.
- Public OpenAPI contract build log in `docs/build-log/2026-06-25-public-openapi-contract.md`.
- Public Problem Details contract build log in `docs/build-log/2026-06-25-problem-details-contract.md`.
- Candidate set contract build log in `docs/build-log/2026-06-25-candidate-set-contract.md`.
- Evidence set contract build log in `docs/build-log/2026-06-26-evidence-set-contract.md`.
- Exclusion contract build log in `docs/build-log/2026-06-26-exclusion-contract.md`.
- Stage candidate contract build log in `docs/build-log/2026-06-26-stage-candidate-contract.md`.
- Retry-aware Problem Details contract build log in `docs/build-log/2026-06-26-problem-details-retry-contract.md`.
- Result row contract build log in `docs/build-log/2026-06-26-result-row-contract.md`.
- Public porting assessment refresh in `docs/build-log/2026-06-26-public-porting-assessment.md`.
- Use-case catalog contract build log in `docs/build-log/2026-06-26-use-case-catalog-contract.md`.
- Ranking group contract build log in `docs/build-log/2026-06-26-ranking-group-contract.md`.
- Use-case taxonomy method build log in `docs/build-log/2026-06-26-use-case-taxonomy-method.md`.
- Fixture use-case alignment build log in `docs/build-log/2026-06-26-fixture-use-case-alignment.md`.
- Public fixture bundle example build log in `docs/build-log/2026-06-26-public-fixture-bundle-example.md`.
- SDK README drift-check build log in `docs/build-log/2026-06-26-sdk-readme-drift-checks.md`.
- Core and schema README drift-check build log in `docs/build-log/2026-06-26-core-schema-readme-drift-checks.md`.
- Scoring stage catalog contract build log in `docs/build-log/2026-06-26-scoring-stage-catalog-contract.md`.
- Scoring stages route contract build log in `docs/build-log/2026-06-26-scoring-stages-route-contract.md`.
- Progress and porting routing refresh in `docs/build-log/2026-06-26-progress-porting-routing.md`.
- Public fixture dispatch build log in `docs/build-log/2026-06-26-public-fixture-dispatch.md`.
- TypeScript fixture-kind parity build log in `docs/build-log/2026-06-26-typescript-fixture-kind-parity.md`.
- Public fixture example README drift-check build log in `docs/build-log/2026-06-26-example-readme-drift-check.md`.
- Public fixture example nested contract README drift-check build log in `docs/build-log/2026-06-26-example-nested-contract-drift-check.md`.
- Python SDK vocabulary export build log in `docs/build-log/2026-06-26-python-sdk-vocabulary-exports.md`.
- Python Problem Details contract build log in `docs/build-log/2026-06-26-python-problem-details-contract.md`.
- Public Problem Details fixture build log in `docs/build-log/2026-06-26-problem-details-fixture.md`.
- Public Problem Details doc URL hardening build log in `docs/build-log/2026-06-26-problem-details-doc-url-hardening.md`.
- Capability fingerprint declared-shape schema hardening build log in `docs/build-log/2026-06-26-capability-fingerprint-shape-schema-hardening.md`.
- Public generated hash schema drift-guard build log in `docs/build-log/2026-06-26-public-hash-schema-drift-guard.md`.
- Recommendation comparability schema hardening build log in `docs/build-log/2026-06-26-recommendation-comparability-schema-hardening.md`.
- `the_call` branch schema hardening build log in `docs/build-log/2026-06-26-the-call-branch-schema-hardening.md`.
- Score-component map hardening build log in `docs/build-log/2026-06-26-score-components-contract-hardening.md`.
- Recommendation envelope validation hardening build log in `docs/build-log/2026-06-26-recommendation-envelope-contract-hardening.md`.
- JSON-object metadata validation hardening build log in `docs/build-log/2026-06-26-json-object-contract-hardening.md`.
- Public doc progress and porting audit in `docs/build-log/2026-06-26-public-doc-progress-porting-audit.md`.
- Primitive and sequence field validation hardening build log in `docs/build-log/2026-06-26-primitive-sequence-contract-hardening.md`.
- Unique request entity-types hardening build log in `docs/build-log/2026-06-26-evaluation-request-entity-type-uniqueness.md`.
- Ranked-entity caveat string hardening build log in `docs/build-log/2026-06-26-ranked-entity-caveat-hardening.md`.
- Public string-field validation hardening build log in `docs/build-log/2026-06-26-public-string-field-contract-hardening.md`.
- Private source port-routing build log in `docs/build-log/2026-06-26-private-source-port-routing.md`.
- Public abstention contract build log in `docs/build-log/2026-06-26-abstention-contract.md`.
- Scoring-stage abstention output alignment build log in `docs/build-log/2026-06-26-scoring-stage-abstention-output.md`.
- Public progress and port-over routing refresh in `docs/build-log/2026-06-26-progress-portover-refresh.md`.
- Port-over inventory refresh build log in `docs/build-log/2026-06-26-port-over-inventory-refresh.md`.
- Public progress and private-side porting recheck build log in `docs/build-log/2026-06-26-public-progress-porting-recheck.md`.
- Public progress, porting, and public-repo safety refresh in `docs/build-log/2026-06-26-public-repo-porting-safety-refresh.md`.
- Current public/private porting workstream refresh in `docs/build-log/2026-06-26-public-porting-workstream-refresh.md`.
- Python SDK recommendation client build log in `docs/build-log/2026-06-26-python-sdk-recommendation-client.md`.
- CLI recommendation command build log in `docs/build-log/2026-06-26-cli-recommendation-command.md`.
- TypeScript recommendation client build log in `docs/build-log/2026-06-26-typescript-recommendation-client.md`.
- MCP recommendation tool build log in `docs/build-log/2026-06-26-mcp-recommendation-tool.md`.
- Python package metadata drift guard build log in `docs/build-log/2026-06-26-python-package-metadata-drift-guard.md`.
- Python SDK metadata route build log in `docs/build-log/2026-06-26-python-sdk-metadata-routes.md`.
- TypeScript SDK metadata route build log in `docs/build-log/2026-06-26-typescript-sdk-metadata-routes.md`.
- CLI metadata command build log in `docs/build-log/2026-06-26-cli-metadata-commands.md`.
- MCP metadata tool build log in `docs/build-log/2026-06-26-mcp-metadata-tools.md`.
- MCP base URL schema hardening build log in `docs/build-log/2026-06-26-mcp-base-url-schema-hardening.md`.
- MCP recommendation request schema hardening build log in `docs/build-log/2026-06-26-mcp-recommendation-request-schema-hardening.md`.
- Package README metadata drift-guard build log in `docs/build-log/2026-06-26-package-readme-metadata-drift-guard.md`.
- Package README exact metadata drift-check build log in `docs/build-log/2026-06-26-package-readme-exact-metadata-drift-check.md`.
- CLI and MCP README exact drift-check build log in `docs/build-log/2026-06-26-cli-mcp-readme-exact-drift-check.md`.
- Schema README exact drift-check build log in `docs/build-log/2026-06-26-schema-readme-exact-drift-check.md`.
- Methods README exact drift-check build log in `docs/build-log/2026-06-26-methods-readme-exact-drift-check.md`.
- Repo structure exact drift-check build log in `docs/build-log/2026-06-26-repo-structure-drift-check.md`.
- Status build-log index drift-check build log in `docs/build-log/2026-06-26-status-build-log-index-drift-check.md`.
- Porting workstream/status drift-check build log in `docs/build-log/2026-06-26-porting-workstream-status-drift-check.md`.
- Public MCP docs boundary drift-check build log in `docs/build-log/2026-06-26-public-mcp-docs-boundary-drift-check.md`.
- TESTS abstention terminology drift-check build log in `docs/build-log/2026-06-26-tests-abstention-terminology-drift-check.md`.
- Private dirty-worktree port-routing recheck build log in `docs/build-log/2026-06-26-private-dirty-worktree-port-routing-recheck.md`.
- `CLAUDE.md` shim drift-check build log in `docs/build-log/2026-06-26-claude-shim-drift-check.md`.
- Scoped `AGENTS.md` coverage drift-check build log in `docs/build-log/2026-06-26-scoped-agents-drift-check.md`.
- Ranked entity axes shape hardening build log in `docs/build-log/2026-06-26-ranked-entity-axes-contract-hardening.md`.
- TypeScript non-empty array parity build log in `docs/build-log/2026-06-26-typescript-nonempty-array-parity.md`.
- TypeScript use-case branch parity build log in `docs/build-log/2026-06-26-typescript-use-case-branch-parity.md`.
- TypeScript `the_call` branch parity build log in `docs/build-log/2026-06-26-typescript-the-call-branch-parity.md`.
- TypeScript recommendation branch parity build log in `docs/build-log/2026-06-26-typescript-recommendation-branch-parity.md`.
- TypeScript recommendation call-state parity build log in `docs/build-log/2026-06-26-typescript-recommendation-call-state-parity.md`.
- Public tuple sequence contract hardening build log in `docs/build-log/2026-06-26-tuple-sequence-contract-hardening.md`.
- Scoring-stage schema uniqueness drift-check build log in `docs/build-log/2026-06-26-scoring-stage-schema-uniqueness-drift-check.md`.
- Scoring-stage ordinal contiguity build log in `docs/build-log/2026-06-26-scoring-stage-ordinal-contiguity.md`.
- Recommendation ranked schema uniqueness hardening build log in `docs/build-log/2026-06-26-recommendation-ranked-schema-uniqueness.md`.
- Ranking group schema uniqueness hardening build log in `docs/build-log/2026-06-26-ranking-group-schema-uniqueness.md`.
- Recommendation group uniqueness hardening build log in `docs/build-log/2026-06-26-recommendation-group-uniqueness.md`.
- Recommendation exclusion uniqueness hardening build log in `docs/build-log/2026-06-26-recommendation-exclusion-uniqueness.md`.
- Recommendation alias exact schema pattern drift-guard build log in `docs/build-log/2026-06-26-recommendation-alias-pattern-drift-guard.md`.
- SDK retry-after parser hardening build log in `docs/build-log/2026-06-26-sdk-retry-after-parser-hardening.md`.
- Recommendation abstention envelope consistency build log in `docs/build-log/2026-06-26-recommendation-abstention-envelope-consistency.md`.
- Recommendation abstention-as-empty-single-scale build log in `docs/build-log/2026-06-26-abstention-no-ranked-answer.md`.
- Recommendation `shortlist_depth` count consistency build log in `docs/build-log/2026-06-26-shortlist-depth-count-consistency.md`.
- Recommendation ranked-row rank contiguity/order build log in `docs/build-log/2026-06-26-recommendation-rank-contiguity.md`.
- Ranked entity freshness date format build log in `docs/build-log/2026-06-26-freshness-date-format-hardening.md`.
- Public temporal field format build log in `docs/build-log/2026-06-26-public-temporal-format-hardening.md`.
- Result row public source URL build log in `docs/build-log/2026-06-26-result-row-source-url-hardening.md`.
- Public confidence interval schema drift-guard build log in `docs/build-log/2026-06-26-confidence-interval-schema-drift-guard.md`.
- Public progress and private-side port-routing refresh build log in `docs/build-log/2026-06-26-public-progress-private-routing-refresh.md`.
- Full-spec status reporting build log in `docs/build-log/2026-06-26-full-spec-status-reporting.md`.
- W6 public reference materializer build log in `docs/build-log/2026-06-26-w6-reference-materializer.md`.
- Private coding-router evidence projection build log in `docs/build-log/2026-06-26-private-coding-router-evidence-projection.md`.
- Private raw coding-router ledger projection build log in `docs/build-log/2026-06-26-private-raw-router-ledger-projection.md`.
- Private Stage-2 scorer-row build log in `docs/build-log/2026-06-26-private-stage2-scorer-rows.md`.
- Private contamination-check build log in `docs/build-log/2026-06-26-private-contamination-checks.md`.
- Private calibration-row build log in `docs/build-log/2026-06-26-private-calibration-rows.md`.
- Private item-response-row build log in `docs/build-log/2026-06-27-private-item-response-rows.md`.
- Private IRT parameter-recovery build log in `docs/build-log/2026-06-27-private-irt-parameter-recovery.md`.
- Private raw-ledger item-response coverage build log in `docs/build-log/2026-06-27-private-raw-ledger-item-response-coverage.md`.
- Private capability-index item-response coverage build log in `docs/build-log/2026-06-27-private-capability-index-item-response-coverage.md`.
- Private capability-index publishable filtered IRT build log in `docs/build-log/2026-06-27-private-capability-index-irt-publishable-filtered.md`.
- Private model cutoff metadata build log in `docs/build-log/2026-06-27-private-model-cutoff-metadata.md`.
- Private pooled IRT fit-scope build log in `docs/build-log/2026-06-27-private-pooled-irt-fit-scope.md`.
- Private IRT SBC diagnostic build log in `docs/build-log/2026-06-27-private-irt-sbc-diagnostic.md`.
- Private semantic contamination probe build log in `docs/build-log/2026-06-27-private-semantic-contamination-probe.md`.
- Private semantic text provenance ingress build log in `docs/build-log/2026-06-27-private-semantic-text-provenance-ingress.md`.
- Private source-owned semantic text metadata build log in `docs/build-log/2026-06-27-private-source-owned-semantic-text-metadata.md`.
- Private golden-eval runner adapter parity build log in `docs/build-log/2026-06-27-private-golden-eval-runner-adapter-parity.md`.
- Private Claude default golden-harness build log in `docs/build-log/2026-06-27-private-claude-default-golden-harness.md`.
- Private Pi runtime health-preflight build log in `docs/build-log/2026-06-27-private-pi-runtime-health-preflight.md`.
- Private Pi runtime image-refresh build log in `docs/build-log/2026-06-27-private-pi-runtime-image-refresh.md`.
- Private golden-eval detached transactions build log in `docs/build-log/2026-06-27-private-golden-eval-detached-transactions.md`.
- Private detached golden refresh build log in `docs/build-log/2026-06-27-private-detached-golden-refresh.md`.
- Private contamination-integrity expansion build log in `docs/build-log/2026-06-27-private-contamination-integrity-expansion.md`.
- Private integrity provenance ingestion build log in `docs/build-log/2026-06-27-private-integrity-provenance-ingestion.md`.
- Private temporal benchmark-release metadata build log in `docs/build-log/2026-06-27-private-temporal-benchmark-release-metadata.md`.
- Private Stage-4 conformal-shortlist build log in `docs/build-log/2026-06-27-private-stage4-conformal-shortlist.md`.
- Private Stage-3 ledger-boundary build log in `docs/build-log/2026-06-27-private-stage3-ledger-boundary.md`.
- Private Stage-3 materialize-time tie-break build log in `docs/build-log/2026-06-27-private-stage3-materialize-time-tiebreak.md`.
- Private current-schema Claude Opus full-20 build log in `docs/build-log/2026-06-27-private-claude-current-schema-full-20.md`.
- Private task-level golden outcome build log in `docs/build-log/2026-06-27-private-task-level-golden-outcomes.md`.
- Private Codex admission-smoke build log in `docs/build-log/2026-06-27-private-codex-admission-smoke.md`.
- Private Codex full-bank active projection build log in `docs/build-log/2026-06-27-private-codex-full-bank-active-projection.md`.
- Private candidate-ledger calibration source build log in `docs/build-log/2026-06-27-private-candidate-ledger-calibration-source.md`.
- Private pooled-calibration Stage-2 consumption build log in `docs/build-log/2026-06-27-private-pooled-calibration-stage2-consumption.md`.
- Private ingestion-worker cache refresh build log in `docs/build-log/2026-06-27-private-ingestion-worker-cache-refresh.md`.
- Private cached recommend API and telemetry read-surface build log in `docs/build-log/2026-06-27-private-api-cache-telemetry-read-surface.md`.
- Private tool-registry capability source-adapter build log in `docs/build-log/2026-06-27-private-tool-registry-capability-source-adapter.md`.
- Private tool-registry source-edge build log in `docs/build-log/2026-06-27-private-tool-registry-source-edges.md`.
- Private tool-registry entry coverage build log in `docs/build-log/2026-06-27-private-tool-registry-entry-coverage.md`.
- Private budget-ledger build log in `docs/build-log/2026-06-27-private-budget-ledger.md`.
- Private source-lifecycle event ledger build log in `docs/build-log/2026-06-27-private-source-lifecycle-events.md`.
- Private staleness lifecycle events build log in `docs/build-log/2026-06-28-private-staleness-lifecycle-events.md`.
- Private W7 POST recommendations route-parity build log in `docs/build-log/2026-06-27-private-w7-post-recommendations-route-parity.md`.
- Private W7 optional API-key auth build log in `docs/build-log/2026-06-28-private-w7-optional-api-key-auth.md`.
- Private coding dogfood verified-run source-adapter build log in `docs/build-log/2026-06-28-private-coding-dogfood-verified-runs.md`.
- Private coding validation outcome source-adapter build log in `docs/build-log/2026-06-28-private-coding-validation-outcomes.md`.
- Private coding validation item-response build log in `docs/build-log/2026-06-28-private-coding-validation-item-responses.md`.
- Private recommend-called fact ingestion build log in `docs/build-log/2026-06-28-private-recommend-called-fact-ingestion.md`.
- Private W7 usage summary build log in `docs/build-log/2026-06-28-private-w7-usage-summary.md`.
- Private visible quota source build log in `docs/build-log/2026-06-28-private-visible-quota-source.md`.
- Private keyed quota enforcement build log in `docs/build-log/2026-06-28-private-keyed-quota-enforcement.md`.
- Private W7 billing settlement build log in `docs/build-log/2026-06-28-private-w7-billing-settlement.md`.
- Private W7 billing readback route build log in `docs/build-log/2026-06-29-private-w7-billing-readback-route.md`.
- Private W7 staging smoke command build log in `docs/build-log/2026-06-28-private-w7-staging-smoke-command.md`.
- Private W8 OTel semantic attributes build log in `docs/build-log/2026-06-28-private-w8-otel-semantic-attributes.md`.
- Private W8 recommend-event daily rollups build log in `docs/build-log/2026-06-28-private-w8-recommend-event-daily-rollups.md`.
- Private W8 recommendation load SLO smoke build log in `docs/build-log/2026-06-28-private-w8-recommendation-load-slo-smoke.md`.
- Private W8 daily observability rollup scheduling build log in `docs/build-log/2026-06-28-private-w8-daily-observability-rollup-scheduling.md`.
- Private W9 WAQC weekly rollup build log in `docs/build-log/2026-06-28-private-w9-waqc-weekly-rollups.md`.
- Private W9 WAQC regression-events build log in `docs/build-log/2026-06-28-private-w9-waqc-regression-events.md`.
- Private W9 regression draft-PR artifact build log in `docs/build-log/2026-06-28-private-w9-regression-draft-pr-artifact.md`.
- Private W9 affiliation-gated draft-PR build log in `docs/build-log/2026-06-28-private-w9-affiliation-gated-draft-pr.md`.
- Private W9 DB-backed regression draft-PR build log in `docs/build-log/2026-06-28-private-w9-db-backed-regression-draft-pr.md`.
- Private W9 regression draft-PR dispatch-readback build log in `docs/build-log/2026-06-28-private-w9-regression-draft-pr-dispatch-readback.md`.
- Private W9 GitHub draft-PR dispatch build log in `docs/build-log/2026-06-28-private-w9-github-draft-pr-dispatch.md`.
- Private W9 community review queue build log in `docs/build-log/2026-06-28-private-w9-community-review-queue.md`.
- Private W9 community listener fetch build log in `docs/build-log/2026-06-28-private-w9-community-listener-fetch.md`.
- Private W9 community digest export build log in `docs/build-log/2026-06-28-private-w9-community-digest-export.md`.
- Private W9 community digest delivery build log in `docs/build-log/2026-06-29-private-w9-community-digest-delivery.md`.
- Private W9 publication-governance tripwires build log in `docs/build-log/2026-06-28-private-w9-publication-governance-tripwires.md`.
- Private W9 publication-governance check-command build log in `docs/build-log/2026-06-28-private-w9-publication-governance-check-command.md`.
- Private W9 operator-affiliations registry build log in `docs/build-log/2026-06-28-private-w9-operator-affiliations-registry.md`.
- Private W9 operator-affiliations check-command build log in `docs/build-log/2026-06-28-private-w9-operator-affiliations-check-command.md`.
- Handoff status refresh build log in `docs/build-log/2026-06-28-handoff-status-refresh.md`.
- Private served-cache source-boundary build log in `docs/build-log/2026-06-28-private-served-cache-source-boundary.md`.
- Private worker-owned source-refresh scheduling build log in `docs/build-log/2026-06-28-private-worker-owned-source-refresh-scheduling.md`.
- Private auxiliary source-refresh projection build log in `docs/build-log/2026-06-28-private-auxiliary-source-refresh-projection.md`.
- Private dogfood bootstrap audit build log in `docs/build-log/2026-06-28-private-dogfood-bootstrap-audit.md`.
- Private dogfood budget cap command build log in `docs/build-log/2026-06-28-private-dogfood-budget-cap-command.md`.
- Private dogfood audit exact provision command build log in `docs/build-log/2026-06-28-private-dogfood-audit-exact-provision-command.md`.
- Private source-owned tombstone events build log in `docs/build-log/2026-06-28-private-source-owned-tombstone-events.md`.

## Current Public Surface

| Surface | Built | Not built yet |
| --- | --- | --- |
| Core contracts | `CapabilityFingerprintInput`, `RawEntry`, `EvaluationRequest`, `CandidateSet`, `StageCandidate`, `EvidenceItem`, `ResultRow`, `UseCase`, `UseCaseCatalog`, `ScoringStage`, `ScoringStageCatalog`, `RankingGroup`, `EvidenceSet`, `Exclusion`, `TheCall`, `Abstention`, `ProblemDetails`, `RankedEntity`, `Recommendation`, public recommendation ID aliases, `EntityRef`, public constants, shared fixture-kind dispatch, strict public score-component maps, strict public calendar-valid freshness/result-run dates, strict public UTC timestamp format, strict public HTTP(S) result source URLs, strict scoring-stage ordinal contiguity, strict recommendation envelope validation, strict recommendation rank contiguity/order, strict recommendation group/exclusion uniqueness, strict abstention/the-call consistency, strict abstention-as-empty-single-scale behavior, strict recommendation `shortlist_depth` count consistency, synthetic fixture factories, and the storage-free public reference materializer. | Source adapters, storage models, graph persistence, private scorer engine, benchmark weights, IRT clusters, Stage-3/4 scorer rows, private cache persistence, trust/security policy runtime. |
| Schemas | JSON Schemas for capability fingerprints, raw entries, evaluation requests, candidate sets, stage candidates, result rows, use-case catalogs, scoring stage catalogs, evidence sets, exclusions, ranked entities with strict public score-component maps and `YYYY-MM-DD` freshness dates, public UTC timestamp fields, recommendations with closed ranking groups, public abstention objects, and empty single-scale abstention branches, evidence items, and retry-aware RFC 9457 Problem Details with public HTTP(S) `doc_url`, plus OpenAPI 3.1.1 for `GET /v1/use-cases`, `GET /v1/scoring-stages`, and `POST /v1/recommendations`, with drift tests against public contracts and pinned public patterns. | Persistence schemas, scorer-runtime schemas, benchmark-weight schemas, and additional route-specific problem types beyond the current public error vocabulary. |
| Python SDK | Package metadata, public re-exports from `evalrank_core`, including `ProblemDetails`, public vocabulary constants, public fixture dispatch helpers, and dependency-free HTTP(S)-only `EvalRankClient` behavior for `GET /v1/use-cases`, `GET /v1/scoring-stages`, and `POST /v1/recommendations` success plus Problem Details errors, with strict numeric `Retry-After` parsing. | Installed package release flow and additional non-fixture client behavior beyond the current public metadata and recommendation routes. |
| TypeScript SDK | Package metadata, public constants, interfaces for current payload contracts, including abstention/the-call state typing, `RecommendationCallState` abstention parity, empty single-scale abstention typing, and dependency-free native `fetch` `EvalRankClient` behavior for `GET /v1/use-cases`, `GET /v1/scoring-stages`, and `POST /v1/recommendations` success plus Problem Details errors, with strict numeric `Retry-After` parsing. | Built JS distribution, published package release flow, and additional non-fixture client behavior beyond the current public metadata and recommendation routes. |
| CLI | Deterministic `fixture fingerprint`, `fixture raw-entry`, `fixture request`, `fixture candidate-set`, `fixture stage-candidate`, `fixture evidence`, `fixture problem`, `fixture result-row`, `fixture use-cases`, `fixture scoring-stages`, `fixture ranking-group`, `fixture evidence-set`, `fixture exclusion`, `fixture recommendation`, explicit HTTP(S)-only `use-cases --base-url ...`, `scoring-stages --base-url ...`, and `recommend --base-url ... --request ...` commands. | Auth, retries, service discovery, workspace/project operations, private DTOs, hosted receipts, or persistence. |
| MCP | Deterministic `evalrank.fixture` adapter, public `evalrank.use_cases` and `evalrank.scoring_stages` tools for explicit HTTP(S)-only metadata success and Problem Details errors, and public `evalrank.recommend` tool for explicit HTTP(S)-only `POST /v1/recommendations` success and Problem Details errors; route tool manifests advertise non-empty HTTP(S) `base_url` inputs, and the recommendation tool manifest advertises the public `EvaluationRequest` shape. | Live MCP server runtime, evidence lookup, scorer tools, auth, retries, service discovery, hosted receipts, persistence, or private data access. |
| Methods | Public scoring-stage vocabulary and storage-free `ScoringStageCatalog`, including `CandidateSet`, `StageCandidate`, `ResultRow`, `EvidenceSet`, `Exclusion`, and `Abstention`; public use-case taxonomy method; and private-boundary notes. | Proprietary weights, thresholds, graders, held-out tasks, and benchmark outputs. |
| Examples | `examples/public_fixture.py` prints the current synthetic public fixture bundle: raw entry, request, candidate set, stage candidate, evidence item, Problem Details, evidence set, result row, use-case catalog, scoring stage catalog, exclusion, and recommendation JSON; its README is drift-checked against the emitted JSON keys plus nested recommendation and scoring-stage contract refs. | Non-fixture demos, live API examples, and private-data examples. |
| Docs | Status tracker, repo structure map with exact drift tests, porting map, route navigation map, package READMEs, build logs, and public/private workstream router. | UI navigation docs; add only when UI routes or deeplinks exist. |

## Progress Snapshot

| Area | State | Next owner |
| --- | --- | --- |
| Repo foundation | Built: public scaffold, package boundaries, license/notice hygiene, root/scoped agent docs, CI, `make check`, deterministic public-boundary scanner, package metadata checks, and package README metadata drift guards. | Open-Core Boundary / CI keeps leak and drift checks current. |
| Public contracts | Built through the current storage-free payload set: fingerprints, raw entries, requests, candidate sets, stage candidates, result rows, use-case catalogs, scoring stage catalogs, ranking groups, evidence sets, exclusions, `the_call`, abstentions, recommendations, public aliases, entity refs, and evidence items. | Public Contracts pins the next standalone payload before SDK/CLI/MCP behavior grows. |
| Schemas and methods | Built: JSON Schemas, OpenAPI route contracts for `GET /v1/use-cases`, `GET /v1/scoring-stages`, and `POST /v1/recommendations`, retry-aware Problem Details, public scoring-stage vocabulary/catalog, and sanitized use-case taxonomy method note. | Methods / Schemas and Public Surface Contracts add only public, product-neutral semantics. |
| Interfaces | Built: Python SDK re-exports plus stdlib metadata/recommendation client behavior, TypeScript public types/constants plus native `fetch` metadata/recommendation client behavior, CLI fixture and recommendation commands, MCP fixture and recommendation tools, runnable public example, and README drift guards. | SDK / CLI / MCP promotes more behavior only after public route/client contracts are pinned. |
| Persistence and hosted ops | Not public: DB bootstrap, Supabase migrations, grants/RLS, live workers, telemetry, auth, billing/admin, deploy config, and credentials. | DB Bootstrap / Syndai Ops, Hosted Ops / GTM, and Secrets / Deploy Ops keep this private until an explicit public cutover exists. |
| Scoring runtime and eval integrity | Public reference materializer is built for supplied storage-free inputs; not public: DB-backed cached rankings, graph persistence, source adapters, private weights, IRT clusters, held-out tasks, graders, traces, answers, and benchmark outputs. | Scoring / Materializer Runtime continues W1-W5 Bayesian calibration, contamination, refreshed evidence coverage, and live IRT coverage in Syndai; Evaluation Integrity keeps held-out material private. |

## Recent Public Delta

| Change | Status | Workstream |
| --- | --- | --- |
| Public fixture outputs were aligned to the public use-case catalog and expanded to include raw entry, candidate set, stage candidate, result row, use-case catalog, scoring stage catalog, ranking group, evidence set, exclusion, and recommendation payloads. | Built here with synthetic fixtures only. | Public Contracts, Examples |
| CLI, MCP, Python SDK, and TypeScript SDK fixture-kind surfaces now share the core public fixture-kind dispatch. | Built here as local deterministic fixture behavior. | SDK / CLI / MCP, Open-Core Boundary / CI |
| `GET /v1/scoring-stages` now exposes the public scoring-stage catalog route contract. | Built here as OpenAPI contract only; no live server, auth, storage, scorer runtime, or hosted deployment behavior was added. | Public Surface Contracts, Methods / Schemas |
| Package and example README drift checks now guard public fixture, SDK, core, and schema surfaces. | Built here with stdlib tests. | Open-Core Boundary / CI, Docs / Public Planning |
| Example README drift checks now include nested public fixture contract refs for recommendation abstention, `the_call`, and scoring-stage output contracts. | Built here as a deterministic docs/example guard; no runtime behavior or private fixture moved. | Open-Core Boundary / CI, Examples |
| Python SDK now re-exports public vocabulary constants from the core source of truth. | Built here as SDK parity with existing schema/core/TypeScript vocabulary; no scorer policy or private runtime behavior moved. | SDK / CLI / MCP, Public Contracts |
| Python core and SDK now expose the public `ProblemDetails` error contract and `PROBLEM_CODES`. | Built here as storage-free public error payload parity with OpenAPI/schema/TypeScript; no hosted auth, tenant context, telemetry, private problem types, or runtime behavior moved. | Public Contracts, Public Surface Contracts, SDK / CLI / MCP |
| Shared public fixtures now include a deterministic `problem` / `sample_problem_details()` payload. | Built here as synthetic RFC 9457 Problem Details fixture parity for core, Python SDK, TypeScript fixture kinds, CLI, MCP, and the runnable example; no hosted error telemetry, tenant context, private problem types, or live service behavior moved. | Public Contracts, Public Surface Contracts, SDK / CLI / MCP, Examples |
| Capability fingerprint schemas now reject empty `declared_capability_shape` objects, matching core behavior. | Built here as schema/core parity hardening for the existing public fingerprint payload; no source adapter, live fetch behavior, private runtime, or DB work moved. | Public Contracts, Methods / Schemas |
| Recommendation schemas now pin `single-scale` versus `kind-grouped` branch shapes, matching core behavior. | Built here as schema/core parity hardening for existing public recommendation envelopes; no scorer normalization, private score semantics, hosted receipt behavior, private runtime, or DB work moved. | Public Contracts, Methods / Schemas |
| Recommendation schemas now pin `the_call` `recommend` versus `abstain` branch shapes, matching core behavior. | Built here as schema/core parity hardening for the existing public decision envelope; no confidence policy, private abstention taxonomy, scorer/runtime behavior, hosted receipt behavior, or DB work moved. | Public Contracts, Methods / Schemas |
| Ranked entity `score_components` now reject non-object maps, blank/non-string names, booleans, and out-of-range values before serialization. | Built here as public contract hardening; no private scorer formula was added. | Public Contracts, Methods / Schemas |
| Recommendation envelopes now reject schema-incompatible metadata and duplicate ranked entities before serialization. | Built here as public contract hardening; no scorer/runtime, route implementation, hosted receipt, or private evidence behavior was added. | Public Contracts, Methods / Schemas |
| Evidence item `metadata` and evaluation request `constraints` now reject non-object, non-string-key, and non-JSON values before serialization. | Built here as public contract hardening; no private evidence lookup, source adapter, or policy behavior was added. | Public Contracts, Methods / Schemas |
| Entity references, freshness dates, request entity-type arrays, ranked-entity integer fields, and caveats now reject schema-incompatible Python values before serialization. | Built here as public contract hardening; no schema expansion, scorer behavior, or private runtime behavior was added. | Public Contracts, Methods / Schemas |
| Evaluation requests now reject duplicate `entity_types`, and the schema pins `uniqueItems`. | Built here as public request-shape hardening; no candidate resolver, scorer behavior, route implementation, DB work, or private source adapter moved. | Public Contracts, Methods / Schemas |
| Ranked entity caveats now reject empty strings before serialization, and the schema pins the same non-empty item rule. | Built here as schema/core parity hardening for the existing public `caveats` array; no scorer behavior, private runtime, or DB work moved. | Public Contracts, Methods / Schemas |
| Capability fingerprint, raw entry, evidence item, evidence set, candidate set, `the_call`, and abstention public string fields now reject truthy non-strings before serialization. | Built here as public contract hardening against existing schemas; no source adapter, scorer/runtime behavior, private evidence lookup, DB work, or hosted operation moved. | Public Contracts, Methods / Schemas |
| Recommendation abstention responses now include a public `Abstention` reason/detail object and ordinary recommendations include `abstention: null`. | Built here as a storage-free response contract; evidence-floor thresholds, confidence policy, private reason taxonomy, scorer/runtime behavior, DB work, and hosted operations were not moved. | Public Contracts, Methods / Schemas, SDK / CLI / MCP |
| The public scoring-stage catalog now names `Abstention` as an output of `ranking-or-abstention`. | Built here as catalog alignment after the public `Abstention` contract; no scorer thresholds, private policy, runtime, or DB behavior moved. | Methods / Schemas, Public Contracts |
| Earlier private-side dirty worktree check found Memphant spec edits and two Memphant plan files, not EvalRank public-port candidates. | Documented here as a routing decision; no private Memphant planning text was copied into the public repo. | Memphant / memory-system workstream, Docs / Public Planning, Open-Core Boundary / CI |
| Earlier private-side dirty worktree recheck found only Memphant planning changes and no uncommitted EvalRank-specific public-port candidate at that time. | Documented here as a public-safe category/path review only; no private file contents were copied. | Memphant / memory-system workstream, Docs / Public Planning, Public Contracts |
| Current private-side dirty worktree refresh found preflight/repo-guidance edits, backend runtime reliability edits, private EvalRank doc-validation edits, and Memphant memory-system planning edits. | Documented as categories only. None should be copied into this public repo now; extract only a future storage-free EvalRank contract or public doc check if one is deliberately identified. | Syndai runtime workstream, Memphant / memory-system workstream, Docs / Public Planning, Open-Core Boundary / CI |
| Current private-side EvalRank source scan classified private specs, build-readiness plans, migration bootstrap, migration guards, doc validators, and UI proof assets. | Documented here as a public-safe routing decision; raw private docs, proof assets, operations scripts, and private migrations were not copied. | Docs / Public Planning, Public Surface Contracts, DB Bootstrap / Syndai Ops, Open-Core Boundary / CI |
| Current private-source inventory counted private EvalRank specs, build plans, proof assets, and backend migration assets, then routed the next port slices. | Documented as categories and owners only. Public work stays on storage-free contracts, deterministic fixtures/checks, route contracts, and sanitized method notes; DB, runtime, UI proofs, hosted ops, and eval-integrity material stay private. | Docs / Public Planning, Public Contracts, Public Surface Contracts, Open-Core Boundary / CI, DB Bootstrap / Syndai Ops, Scoring / Materializer Runtime, Evaluation Integrity |
| GitHub repo security metadata was checked for the public repo. | Public visibility, secret scanning, push protection, and Dependabot security updates are enabled; the local boundary scanner remains the required gate because platform scanning is only a backstop. | Open-Core Boundary / CI, Secrets / Deploy Ops |
| Public security reporting and porting safety docs were refreshed after rechecking the public repo metadata and private-side dirty worktree. | Built here as docs-only guidance: public reports must not include secrets/private fixtures/customer data, exposed secrets must be treated as compromised, and next ports stay on public contracts/checks rather than private Memphant planning. | Docs / Public Planning, Open-Core Boundary / CI, Secrets / Deploy Ops |
| Python SDK added a dependency-free recommendation client for the existing public `POST /v1/recommendations` route contract. | Built here as HTTP(S)-only stdlib HTTP/JSON behavior; no auth, retries, hosted receipts, tenant context, private DTOs, service discovery, local file URLs, or production evidence lookup moved. | SDK / CLI / MCP, Public Surface Contracts |
| CLI added an explicit recommendation command for the existing public `POST /v1/recommendations` route contract. | Built here as HTTP(S)-only file/stdin JSON plumbing around the public Python SDK client; no hidden network calls, auth, retries, environment-variable defaults, hosted receipts, private DTOs, database work, or production evidence lookup moved. | SDK / CLI / MCP, Public Surface Contracts |
| TypeScript SDK added a dependency-free recommendation client for the existing public `POST /v1/recommendations` route contract. | Built here as HTTP(S)-only native `fetch` behavior with local package runtime tests; no auth, retries, hosted receipts, tenant context, private DTOs, service discovery, environment-variable defaults, local file URLs, or production evidence lookup moved. | SDK / CLI / MCP, Public Surface Contracts |
| MCP added an explicit recommendation tool for the existing public `POST /v1/recommendations` route contract. | Built here as HTTP(S)-only JSON plumbing around the public Python SDK client; no hidden network calls, auth, retries, environment-variable defaults, hosted receipts, private DTOs, database work, or production evidence lookup moved. | SDK / CLI / MCP, Public Surface Contracts |
| Python package metadata drift guard now pins public package dependency edges and package hygiene metadata. | Built here as stdlib `tomllib` tests over existing `pyproject.toml` files; no packaging release or publishing workflow was added. | Open-Core Boundary / CI |
| Package READMEs now carry manifest metadata and a deterministic drift guard. | Built here as local stdlib tests that bind public Python package READMEs and the TypeScript SDK README to current package names, imports, dependencies, entrypoints, license, module type, and private publish status; no publish workflow, private package index, credentials, or hosted deployment behavior was added. | Open-Core Boundary / CI, Docs / Public Planning |
| Package README metadata drift checks now reject stale extra package metadata lines. | Built here as a stdlib exact-set check over each public README metadata block and current package manifests; no runtime behavior, packaging release flow, private package index, credentials, or hosted deployment behavior moved. | Open-Core Boundary / CI, Docs / Public Planning |
| CLI and MCP README drift checks now reject stale extra public commands, fixture kinds, and tool names. | Built here as stdlib regex checks over public docs and existing public fixture/tool constants; no runtime behavior, private services, or new package surface moved. | Open-Core Boundary / CI, SDK / CLI / MCP, Docs / Public Planning |
| Schema README drift checks now reject stale extra schema/OpenAPI filenames. | Built here as a stdlib regex check over public schema docs and current public schema files; no schema shape, runtime behavior, or private material moved. | Open-Core Boundary / CI, Methods / Schemas, Docs / Public Planning |
| Methods README drift checks now reject stale extra method-note filenames. | Built here as a stdlib exact-set check over public method note files and `methods/README.md`; no private methodology, scorer weights, thresholds, benchmark data, runtime behavior, or new method content moved. | Open-Core Boundary / CI, Methods / Schemas, Docs / Public Planning |
| Ranked entity `axes.evidence` is now closed in JSON Schema and mirrored in the TypeScript SDK type. | Built here as schema/type parity with the existing Python `RankedEntity.to_dict()` output; no private evidence scoring, weights, formulas, scorer runtime, or persistence behavior moved. | Public Contracts, Methods / Schemas, SDK / CLI / MCP |
| TypeScript SDK now exposes `NonEmptyArray<T>` for already-public schema `minItems: 1` arrays. | Built here as compile-time parity for existing Python and JSON Schema contracts; no runtime validation, new route behavior, private data, scorer logic, or persistence behavior moved. | SDK / CLI / MCP, Public Contracts |
| TypeScript SDK now models `UseCase` as a ranked/veto-overlay discriminated union. | Built here as compile-time parity for existing Python and JSON Schema use-case branch rules; no private rank-policy tuning, scorer runtime, hosted behavior, or persistence moved. | SDK / CLI / MCP, Public Contracts |
| TypeScript SDK now models `TheCall` as a `recommend`/`abstain` discriminated union. | Built here as compile-time parity for existing Python and JSON Schema branch rules; no private confidence policy, thresholds, scorer runtime, hosted behavior, or persistence moved. | SDK / CLI / MCP, Public Contracts |
| TypeScript SDK now models `Recommendation` as a `single-scale`/`kind-grouped` discriminated union. | Built here as compile-time parity for existing Python and JSON Schema comparability branch rules; no scorer normalization, private score semantics, hosted receipt behavior, runtime, or DB work moved. | SDK / CLI / MCP, Public Contracts |
| TypeScript SDK `RecommendationCallState` now includes the abstention branch. | Built here as compile-time parity for the existing public `the_call`/`abstention` state; no private abstention policy, scorer/runtime behavior, hosted receipt behavior, or DB work moved. | SDK / CLI / MCP, Public Contracts |
| Python SDK added metadata route helpers for the existing public `GET /v1/use-cases` and `GET /v1/scoring-stages` route contracts. | Built here as explicit HTTP(S)-only stdlib GET behavior sharing public JSON and Problem Details handling; no auth, retries, service discovery, environment-variable defaults, hosted receipts, private DTOs, database work, or production evidence lookup moved. | SDK / CLI / MCP, Public Surface Contracts |
| TypeScript SDK added metadata route helpers for the existing public `GET /v1/use-cases` and `GET /v1/scoring-stages` route contracts. | Built here as explicit HTTP(S)-only native `fetch` GET behavior sharing public JSON and Problem Details handling; no auth, retries, service discovery, environment-variable defaults, hosted receipts, private DTOs, database work, or production evidence lookup moved. | SDK / CLI / MCP, Public Surface Contracts |
| CLI added explicit metadata commands for the existing public `GET /v1/use-cases` and `GET /v1/scoring-stages` route contracts. | Built here as explicit HTTP(S)-only Python SDK plumbing with JSON stdout and Problem Details stderr; no hidden network calls, auth, retries, environment-variable defaults, hosted receipts, private DTOs, database work, or production evidence lookup moved. | SDK / CLI / MCP, Public Surface Contracts |
| MCP added explicit metadata tools for the existing public `GET /v1/use-cases` and `GET /v1/scoring-stages` route contracts. | Built here as explicit HTTP(S)-only Python SDK plumbing with MCP text JSON results and Problem Details tool errors; no hidden network calls, auth, retries, environment-variable defaults, hosted receipts, private DTOs, database work, or production evidence lookup moved. | SDK / CLI / MCP, Public Surface Contracts |
| MCP route tool schemas now advertise non-empty HTTP(S) `base_url` inputs. | Built here as manifest/runtime boundary parity for existing public metadata and recommendation tools; no service discovery, auth, retries, hosted receipts, private services, or database work moved. | SDK / CLI / MCP, Public Surface Contracts |
| MCP recommendation tool schema now advertises the public `EvaluationRequest` input shape. | Built here as manifest/OpenAPI parity for the existing public recommendation route; server-side validation remains authoritative, and no auth, private DTOs, hosted receipts, service discovery, database work, or scorer runtime moved. | SDK / CLI / MCP, Public Surface Contracts |
| `CandidateSet` and `EvidenceSet` now reject mutable list-backed sequence inputs in the Python core. | Built here as public contract immutability hardening for existing storage-free payloads; JSON serialization still emits public arrays, empty evidence sets remain valid, and no graph lookup, evidence lookup, scorer runtime, DB work, or private source adapter moved. | Public Contracts |
| Scoring-stage catalog schema tests now pin uniqueness on stages and stage contract refs. | Built here as a drift guard for the existing public schema; no new scorer stages, scoring formulas, runtime, DB work, or private methodology moved. | Methods / Schemas, Open-Core Boundary / CI |
| Scoring-stage catalogs now reject non-contiguous ordinals in the Python core. | Built here as public method-stage order hardening; JSON Schema remains structural, and no scorer formulas, runtime, DB work, telemetry, or private methodology moved. | Public Contracts, Methods / Schemas |
| Recommendation `ranked` now pins `uniqueItems` in JSON Schema. | Built here as schema/core parity for the existing single-scale recommendation contract; no scorer normalization, private score semantics, hosted receipt behavior, runtime, or DB work moved. | Public Contracts, Methods / Schemas |
| Recommendation `RankingGroup.ranked` now pins `uniqueItems` in JSON Schema. | Built here as schema/core parity for the existing grouped recommendation contract; no cross-kind normalization, scorer runtime, private score semantics, DB work, or hosted receipt behavior moved. | Public Contracts, Methods / Schemas |
| Recommendation `exclusions` now pin `uniqueItems` in JSON Schema and reject duplicate exclusion rows in the Python core. | Built here as public response-shape hardening for existing storage-free exclusions; no gate policy, private safety taxonomy, scorer runtime, DB work, hosted operation, or telemetry moved. | Public Contracts, Methods / Schemas |
| Python and TypeScript SDKs now parse `Retry-After` strictly as integer seconds. | Built here as public client error-header hardening for existing Problem Details behavior; malformed headers become absent retry hints, and no retry loop, auth, hosted receipt, service discovery, telemetry, or private route behavior moved. | SDK / CLI / MCP, Public Surface Contracts |
| Status docs now mention every current porting workstream from `docs/PORTING.md`. | Built here as a deterministic public-doc guard so progress snapshots retain the routing vocabulary needed for future port decisions; no private plan text, private worktree contents, DB work, hosted ops, telemetry, or runtime behavior moved. | Open-Core Boundary / CI, Docs / Public Planning |
| Public MCP docs no longer advertise private evidence lookup. | Built here as a deterministic public-doc boundary guard over root and MCP package docs; no live MCP server runtime, evidence lookup, private service access, DB work, hosted ops, telemetry, or runtime behavior moved. | Open-Core Boundary / CI, SDK / CLI / MCP, Docs / Public Planning |
| `TESTS.md` now uses the current abstention-as-empty-single-scale contract name. | Built here as a deterministic public-doc terminology guard; no contract shape, private scorer policy, DB work, hosted ops, telemetry, or runtime behavior moved. | Open-Core Boundary / CI, Docs / Public Planning, Public Contracts |
| Latest private-side dirty worktree recheck now routes 96 private paths by category only. | Documented here as a public-safe routing decision: current private work spans repo guidance/preflight/test-map docs, backend runtime reliability/orchestration/tools, private backend tests, private DB migration/bootstrap, coding runtime/governance docs, private plans, Memphant planning, and mobile/web auth or connection UI. No private file contents or operational details were copied. | Docs / Public Planning, Syndai runtime workstream, DB Bootstrap / Syndai Ops, Memphant / memory-system workstream, Hosted Ops / GTM |
| Recommendation abstention state now requires `the_call` and `abstention` to agree. | Built here as public envelope consistency in core, JSON Schema, and TypeScript types; no evidence-floor threshold, private abstention taxonomy, scorer/runtime behavior, DB work, or hosted operation moved. | Public Contracts, Methods / Schemas, SDK / CLI / MCP |
| Recommendation abstention responses must be empty single-scale responses. | Built here as public response-shape consistency in core, JSON Schema, and TypeScript types; no scorer threshold, evidence-floor policy, private reason taxonomy, runtime behavior, DB work, or hosted operation moved. | Public Contracts, Methods / Schemas, SDK / CLI / MCP |
| Recommendation `shortlist_depth` now must match the returned ranked row count. | Built here as Python core response-shape consistency for single-scale and kind-grouped recommendations; JSON Schema stays structural, and no scorer threshold, private ranking policy, runtime behavior, DB work, hosted operation, or telemetry moved. | Public Contracts |
| Remaining live scorer calls, hosted receipts, auth, persistence, graph lookup, source adapters, and eval-integrity material were not ported. | Keep private or out until each item has a public contract and no secret/private-data dependency. | Public Surface Contracts, Scoring / Materializer Runtime, DB Bootstrap / Syndai Ops, Hosted Ops / GTM, Evaluation Integrity |

## In Progress

- W6 public reference materializer is validated locally under `docs/superpowers/plans/2026-06-26-w6-reference-materializer.md`, and private Syndai proof-of-spine cache rows are verified from real capability-search, capability-index, tool-registry capability metadata, tool-registry entry metadata tracking, capability-index criterion responses with publishable filtered IRT, active coding-router evidence, raw coding-router ledger sources, live coding-validation outcome rows, candidate-ledger calibration source, empirical-Bayes calibration posterior rows, item-response rows across active evidence plus raw-ledger/candidate-ledger sources, source-owned task-level golden outcomes, a pooled Bayesian 2PL IRT batch path with publishable SBC diagnostics, explicit pooled-calibration Stage-2 consumption, replayable ingestion-worker cache refresh, source-snapshot-lineage CAS, append-only `recommend.called` fact-table persistence, keyed current-period usage aggregation, keyed quota enforcement, private async billing settlement/readback plus authenticated readback route, Stage-4 conformal-shortlist rows, guarded materialize-time Stage-3 tie-break behavior, expanded contamination-integrity rows, cross-adapter live exact-overlap provenance, benchmark-release metadata, model cutoff metadata, redacted semantic-text ingress, source-owned semantic text arrays, detached live refresh, safe promotion carry-forward, current-schema Claude Opus 20/20 active promotion, private Pi runtime image refresh, full Pi runtime/provenance plus review-governance coverage that failed promotion, Codex 19/20 full-bank active projection, and Sonnet 12/20 candidate-only calibration projection. Next work is hosted/staging proof, live Stripe settlement/readback, terminal dogfood source rows after Sentry setup, broader analytics transport, and Pi full-bank behavior gates. DB/source adapters, private scorer weights, thresholds, telemetry, and live evidence-graph materialization stay in Syndai.
- Public/private porting triage and workstream routing.
- Current source of truth is split between:
  - Python contracts in `packages/core/src/evalrank_core/contracts.py`
  - Public JSON Schemas in `schemas/`
  - Public porting decisions in `docs/PORTING.md`
- Private Syndai build-readiness docs and operational plans, summarized here only when public-safe.
- Latest private-side dirty check: Syndai currently has 96 dirty paths classified by category only: 5 repo guidance/preflight/test-map doc paths, 26 backend runtime reliability/orchestration/tool paths, 31 private backend regression-test paths, 1 private DB migration/bootstrap path, 3 coding runtime/governance doc paths, 5 private plan/architecture-note paths, 19 Memphant memory-system planning paths, and 5 mobile/web auth or connection UI paths. Treat these as private workstream inputs only unless a later task extracts an explicit EvalRank storage-free contract or public doc check.
- Latest private-source inventory: the Syndai checkout contains 25 private EvalRank spec docs, 6 private EvalRank build-plan docs, 18 private UI/proof assets, and 5 backend migration/guard/test assets. These are routing inputs only. Public ports should continue as explicit storage-free contracts, schemas, synthetic fixtures, deterministic checks, public route contracts, or sanitized method notes.
- Latest public-repo safety review: `siddmax/evalrank` is public with GitHub secret scanning, push protection, and Dependabot security updates enabled; local public-boundary checks remain mandatory because platform scanning is only a backstop.
- Latest port review: storage-free contracts, schemas, synthetic fixtures, public SDK/CLI/MCP boundaries, public route contracts, deterministic public-boundary checks, and sanitized method notes can move here. Public recommendation identifier aliases, storage-free `RawEntry`, public `CandidateSet`, public `StageCandidate`, public `EvidenceItem`, public `ResultRow`, public `UseCaseCatalog`, public `ScoringStageCatalog`, public `RankingGroup`, public `EvidenceSet`, public `Exclusion`, structured public `the_call`, public `Abstention`, the first storage-free OpenAPI route contracts, retry-aware public Problem Details error shape, Python/TypeScript SDK metadata route helpers, CLI metadata commands, and MCP metadata tools have moved. The private-side planning scan is summarized in `docs/PORTING.md` by workstream, not copied. Private EvalRank specs, build-readiness plans, UI proof assets, doc validators, DB bootstrap, Supabase migrations, live deploy wiring, telemetry, billing/admin/GTM, private integrations, credentials, production data, HMAC/secret-backed hosted IDs, source adapters, live fetch behavior, graph lookup, live evidence lookup, evidence ledger runtime, cross-kind score normalization, benchmark weights, IRT fit clusters, thin-coverage/synthesis policy details, private scorer rows, gate policy, private reason taxonomy, scorer thresholds, private problem types, and held-out evaluation material stay private unless a later task extracts a concrete public contract.

## Current Port-Over Snapshot

| Change or source area | Public status | Owning workstream |
| --- | --- | --- |
| Public repository scaffold, package boundaries, CI, license/notice hygiene, and boundary scanner | Ported here | Open-Core Boundary / CI |
| Storage-free core payloads: capability fingerprint, methodology version, raw entry, evaluation request, candidate set, stage candidate, evidence item, result row, use-case catalog, ranking group, evidence set, exclusion, `the_call`, abstention, ranked entity, recommendation, recommendation aliases, strict public calendar-valid freshness/result-run dates, strict public UTC timestamp format, strict public HTTP(S) result source URLs, strict recommendation envelope validation, strict recommendation rank contiguity/order, strict recommendation exclusion uniqueness, strict abstention-as-empty-single-scale behavior, strict recommendation `shortlist_depth` count consistency, tuple-backed candidate/evidence set validation, and entity reference | Ported here | Public Contracts |
| Public JSON Schemas and schema drift tests for current payloads | Ported here, including retry-aware Problem Details extensions | Methods / Schemas, Public Contracts, Public Surface Contracts |
| Synthetic fixtures, runnable public example, CLI fixture/metadata/recommend commands, MCP fixture/metadata/recommend tools, Python SDK re-exports/client, and TypeScript public types/client | Ported here, including raw-entry, candidate-set, stage-candidate, result-row, problem, use-cases, scoring-stages, ranking-group, evidence-set, exclusion fixture surfaces, first recommendation clients/tools, Python/TypeScript SDK metadata route helpers, CLI metadata commands, MCP metadata tools, and README drift guards | SDK / CLI / MCP, Examples, Open-Core Boundary / CI |
| Public package README metadata | Ported here as concise package metadata blocks plus deterministic manifest drift tests for public distribution names, imports, dependencies, entrypoint, license, TypeScript module/type metadata, and private publish status. | Open-Core Boundary / CI, Docs / Public Planning |
| Public scoring-stage vocabulary, catalog, and private-boundary notes | Ported here, including contiguous public stage ordinals, `ScoringStageCatalog`, `CandidateSet`, `StageCandidate`, `ResultRow`, `EvidenceSet`, `Exclusion`, `Abstention`, and the use-case taxonomy method | Methods / Schemas |
| `RawEntry` ingestion-normalization contract | Ported here as a storage-free synthetic fixture contract | Public Contracts |
| `CandidateSet` candidate-resolution contract | Ported here as a storage-free list of public `EntityRef` candidates; Python core inputs must be tuple-backed before serialization to public arrays | Public Contracts, Methods / Schemas |
| `StageCandidate` Stage-1 retrieval row | Ported here as a storage-free candidate fingerprint plus public entity, fused score, RRF ranks, and retrieval provenance | Public Contracts, Methods / Schemas |
| `ResultRow` ingested result provenance envelope | Ported here as a storage-free benchmark/result row with public flags and verification state | Public Contracts, Methods / Schemas |
| `EvidenceSet` evidence-attachment contract | Ported here as a storage-free list of public `EvidenceItem` rows; empty tuple-backed inputs are allowed for no-evidence or abstention paths before serialization to public arrays | Public Contracts, Methods / Schemas |
| `Exclusion` exclusions-with-reasons contract | Ported here as a storage-free subject plus public reason/detail row | Public Contracts, Methods / Schemas |
| Structured public `the_call` / decision-confidence shape and public `Abstention` reason/detail object | Ported here as storage-free nested recommendation contracts | Public Contracts, Methods / Schemas |
| REST/OpenAPI source of truth | Route contracts ported for `GET /v1/use-cases`, `GET /v1/scoring-stages`, and `POST /v1/recommendations`; public errors use reusable RFC 9457 Problem Details responses plus retry and rate-limit headers | Public Surface Contracts |
| Use-case taxonomy and `/v1/use-cases` route shape | Ported here as storage-free public taxonomy only: id/slug, name, one-line definition, entity-kind spans, ranked-vs-overlay policy, fixture, schema, SDK/CLI/MCP parity, OpenAPI route contract, and sanitized method note | Public Contracts, Public Surface Contracts, SDK / CLI / MCP, Methods / Schemas |
| Recommendation comparability and ranking groups | Ported here as storage-free response shapes only: single-scale recommendations now require unique ranked rows, and `kind-grouped` recommendations contain unique closed `RankingGroup` rows with within-kind ranked entities and rationale | Public Contracts, Methods / Schemas, SDK / CLI / MCP |
| Use-case benchmark weights, IRT cluster crosswalk, confidence policies, and synthesis/coverage rules | Keep private for now; later publish only sanitized method notes that omit weights, held-out tasks, proprietary tuning, and private benchmark outputs | Methods / Schemas, Scoring / Materializer Runtime, Evaluation Integrity |
| Supabase schema bootstrap, migrations, grants/RLS, live DB checks, and shared Finn/Supabase operations | Keep private | DB Bootstrap / Syndai Ops |
| Syndai EvalRank migration guard and runner tests | Keep private with the current DB bootstrap; port only a public migration-policy checklist if EvalRank later owns persistence. | DB Bootstrap / Syndai Ops, Open-Core Boundary / CI |
| Syndai EvalRank doc-validation rules for private specs and build plans | Do not copy private spec checks. Distill only public-facing invariants into this repo when the matching public docs or plans exist. | Docs / Public Planning, Open-Core Boundary / CI |
| Current Syndai dirty worktree categories | Preflight/repo-guidance and backend runtime reliability edits stay in Syndai; Memphant spec/plan edits stay in the memory-system workstream; EvalRank-named doc-validator material remains private input only. Port only an explicit public contract or public doc check extracted from those areas. | Syndai runtime workstream, Memphant / memory-system workstream, Docs / Public Planning, Public Contracts only after explicit extraction |
| Private EvalRank UI proof assets and hosted-product design docs | Keep private until UI routes or public product docs are intentionally added; then port only synthetic screenshots or public-safe docs. | Public Surface Contracts, Hosted Ops / GTM |
| Deterministic scorer, materializer, entity graph persistence, and evidence-ledger runtime | Public reference materializer ported for supplied storage-free inputs only; real cache persistence, graph lookup, source adapters, scorer policy, and evidence-ledger runtime remain private | Scoring / Materializer Runtime |
| Hosted receipts, HMAC-backed IDs, auth, billing/admin/GTM, telemetry, deploy config, and credentials | Keep private | Hosted Ops / GTM, Secrets / Deploy Ops |
| Held-out tasks, graders, answers, traces, benchmark outputs, and judge-calibration material | Never port | Evaluation Integrity |

## Porting Queue

| Priority | Workstream | Destination | Public handling |
| --- | --- | --- | --- |
| 1 | Public Contracts | This repo | First raw entry, request, candidate set, stage candidate, result row, use-case catalog, ranking group, evidence set, exclusion, `the_call`, abstention, recommendation, recommendation alias, and entity/evidence slices ported; extend only for new public payload contracts. |
| 2 | Methods / Schemas | This repo | Public scoring-stage vocabulary/catalog, contiguous stage-order invariant, and use-case taxonomy method note ported; add details only after private material is removed. |
| 3 | SDK / CLI / MCP | This repo | Python and TypeScript SDK metadata route helpers, CLI metadata commands, MCP metadata tools, Python SDK/TypeScript SDK/CLI/MCP first recommendation clients/tools, and fixture surfaces are ported; richer behavior waits for pinned public contracts. |
| 4 | Docs / Public Planning | This repo | Current status, repo structure, porting docs, and first runnable example are public-safe; keep updating them with each port. |
| 5 | Public Surface Contracts | This repo | First OpenAPI route contracts and retry-aware public error responses are ported; add more routes only when concrete public contracts exist, and keep private DTOs and hosted auth outside. |
| 6 | DB Bootstrap / Syndai Ops | Syndai repo | Keep Supabase migrations, live bootstrap, grants/RLS, and operational checks private during incubation. |
| 7 | Scoring / Materializer Runtime | Private incubation first | Split reusable deterministic core before porting; private data, proprietary weights, and live workers stay out. |
| 8 | Evaluation Integrity | Private eval systems | Keep held-out tasks, graders, answers, traces, and benchmark results private. |
| 9 | Hosted Ops / GTM | Private hosted systems | Keep billing, admin, telemetry, vendor intent, and account operations out of this repo. |
| 10 | Secrets / Deploy Ops | Private ops only | Keep credentials, Doppler config, live project refs, and deploy environment files out of Git history. |

## Next

- Public Contracts workstream: pin the next storage-free payload contract before adding more SDK/CLI/MCP behavior; keep hardening existing public envelopes when schema/core drift is found. Do not treat private runtime worker changes as public payload contracts.
- SDK / CLI / MCP workstream: Python SDK, TypeScript SDK, CLI, and MCP now cover the public metadata routes; Python/TypeScript SDKs, CLI, and MCP cover the first `POST /v1/recommendations` behavior. Promote richer behavior only after each public client contract is pinned.
- Public Surface Contracts workstream: extend OpenAPI only for concrete public routes or route-specific problem types beyond the current shared retry vocabulary.
- Scoring / Materializer Runtime workstream: public storage-free reference materializer is separable; continue the next W1-W5 Bayesian calibration, contamination, refreshed evidence coverage, and live IRT coverage in Syndai against the private cache/evidence graph.
- Docs / Public Planning workstream: keep `docs/STATUS.md`, `docs/PORTING.md`, `docs/REPO_STRUCTURE.md`, package READMEs, and build logs aligned in the same change.
- Secrets / Deploy Ops workstream: keep relying on GitHub push protection and secret scanning as backstops, but block leaks earlier with local boundary checks and private review before every public port.
- Update `NAVIGATION.md` when EvalRank adds or changes public API routes, UI routes, deeplinks, or navigation-critical docs.

## Left

- Public repo: additional storage-free contracts, schemas, SDK/CLI/MCP behavior, public examples, additional routes/problem types, UI navigation docs, and reproducible public evaluation fixtures.
- Private/Syndai or hosted systems: data-plane tables, Supabase migrations, entity graph persistence, evidence ledger, Stage-3/4 scorer/materializer rows, private trust/security policy runtime, real cached engine materializer, production telemetry, governance operations, billing/admin, and GTM fleet.
- Private evaluation systems: held-out tasks, graders, answers, traces, benchmark outputs, and proprietary ranking experiments.

## Update Rules

- Update this file when a feature lands, a wave gate changes, or an item moves between `Next`, `In Progress`, and `Built`.
- Keep dated build logs under `docs/build-log/` for immutable-ish snapshots; keep this file current.
