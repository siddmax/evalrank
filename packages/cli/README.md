# EvalRank CLI

Command-line boundary for public EvalRank workflows.

Current command:

```sh
evalrank fixture evidence
evalrank fixture recommendation
```

Both commands write deterministic public JSON fixtures and perform no network or database work.
