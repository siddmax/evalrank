# EvalRank Core

Reference Python package for public EvalRank evidence and scoring contracts.

Use `evalrank_core.fixtures.sample_evaluation_request()`, `sample_recommendation()`, and `sample_evidence_item()` for public examples and contract tests.
